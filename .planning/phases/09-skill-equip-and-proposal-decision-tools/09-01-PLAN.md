---
phase: 09-skill-equip-and-proposal-decision-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/skill/services/player_skill_tool_service.py
  - src/ai_rpg_world/application/llm/services/executors/world_executor.py
  - tests/application/skill/services/test_player_skill_tool_service.py
  - tests/application/llm/test_tool_command_mapper.py
autonomous: true
requirements:
  - SKTL-01
  - SKPR-01
  - SKPR-02
user_setup: []
must_haves:
  truths:
    - Phase 9 should close the execution path for already-public tools rather than redesigning tool contracts.
    - The LLM-facing skill API should remain cohesive behind `PlayerSkillToolApplicationService`, not split between facade and direct command-service calls.
    - `WorldToolExecutor` must own user-facing success/failure DTO production and continue delegating exceptions to `exception_result(...)`.
    - Proposal accept remains an atomic operation at the service layer: progress update plus loadout reflect together or fail together.
  artifacts:
    - path: src/ai_rpg_world/application/skill/services/player_skill_tool_service.py
      provides: LLM-facing facade methods for equip / accept proposal / reject proposal
      min_lines: 60
    - path: src/ai_rpg_world/application/llm/services/executors/world_executor.py
      provides: Skill equip and proposal-decision handlers wired into the existing world executor
      min_lines: 120
    - path: tests/application/skill/services/test_player_skill_tool_service.py
      provides: Focused facade delegation coverage for the new skill tool methods
      min_lines: 100
  key_links:
    - from: src/ai_rpg_world/application/skill/services/player_skill_tool_service.py
      to: src/ai_rpg_world/application/skill/services/skill_command_service.py
      via: New facade methods must construct the existing command DTOs instead of bypassing the command service
      pattern: EquipPlayerSkillCommand|AcceptSkillProposalCommand|RejectSkillProposalCommand
    - from: src/ai_rpg_world/application/llm/services/executors/world_executor.py
      to: tests/application/llm/test_tool_command_mapper.py
      via: Mapper-level execution tests should prove the new handlers are reachable through the existing executor map
      pattern: skill_equip|skill_accept_proposal|skill_reject_proposal
---

<objective>
Close the missing execution seam for Phase 9 skill tools.

Purpose: make `skill_equip`, `skill_accept_proposal`, and `skill_reject_proposal` callable through the same world-executor path already used by `combat_use_skill`.
Output: expanded skill tool facade, new world executor handlers, and baseline mapper/facade tests that prove the tools execute.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-skill-equip-and-proposal-decision-tools/09-CONTEXT.md
@.planning/phases/09-skill-equip-and-proposal-decision-tools/09-RESEARCH.md
@src/ai_rpg_world/application/skill/services/player_skill_tool_service.py
@src/ai_rpg_world/application/skill/services/skill_command_service.py
@src/ai_rpg_world/application/skill/contracts/commands.py
@src/ai_rpg_world/application/llm/services/executors/world_executor.py
@src/ai_rpg_world/application/llm/services/tool_command_mapper.py
@tests/application/llm/test_tool_command_mapper.py
@tests/application/skill/services/test_skill_command_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend the LLM-facing skill facade</name>
  <files>src/ai_rpg_world/application/skill/services/player_skill_tool_service.py, tests/application/skill/services/test_player_skill_tool_service.py</files>
  <action>Add facade methods for equip, proposal accept, and proposal reject that build the existing command DTOs and delegate to `SkillCommandService`. Keep the facade thin and free of result-message formatting.</action>
  <verify>pytest tests/application/skill/services/test_player_skill_tool_service.py -x</verify>
  <done>The skill tool facade exposes the three Phase 9 actions with command-level delegation coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add world executor handlers for the three Phase 9 tools</name>
  <files>src/ai_rpg_world/application/llm/services/executors/world_executor.py</files>
  <action>Update service validation to require the new facade callables and register handler methods for `skill_equip`, `skill_accept_proposal`, and `skill_reject_proposal`. Keep success messages minimal for now; detailed label-driven wording lands in the next plan.</action>
  <verify>pytest tests/application/llm/test_tool_command_mapper.py -x -k "skill and (equip or proposal)"</verify>
  <done>The existing world executor can execute all Phase 9 tools instead of only `combat_use_skill`.</done>
</task>

<task type="auto">
  <name>Task 3: Prove mapper-level reachability for Phase 9 actions</name>
  <files>tests/application/llm/test_tool_command_mapper.py</files>
  <action>Extend mapper tests so the new tool names route through the world executor, call the facade with canonical arguments, and return successful `LlmCommandResultDto` objects.</action>
  <verify>pytest tests/application/llm/test_tool_command_mapper.py -x -k "skill and (equip or accept_proposal or reject_proposal)"</verify>
  <done>The LLM execution path reaches the new handlers and facade methods from `ToolCommandMapper.execute(...)`.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/application/skill/services/test_player_skill_tool_service.py -x`
- [ ] `pytest tests/application/llm/test_tool_command_mapper.py -x -k "skill and (equip or proposal)"`
- [ ] `PlayerSkillToolApplicationService` remains a thin command-building facade
- [ ] `WorldToolExecutor` validates and registers all four skill tool methods (`use_skill` plus Phase 9 additions)
- [ ] No awakened-mode execution work is introduced in this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 9 tools have a real execution seam through the existing facade/executor stack
  </success_criteria>

<output>
After completion, create `.planning/phases/09-skill-equip-and-proposal-decision-tools/09-01-SUMMARY.md`
</output>
