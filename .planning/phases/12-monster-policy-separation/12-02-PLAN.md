---
phase: 12-monster-policy-separation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/ai_rpg_world/application/world/services/monster_lifecycle_survival_coordinator.py
  - src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - tests/application/world/services/test_monster_lifecycle_survival_coordinator.py
  - tests/application/world/services/test_hunger_migration.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - WSPOL-01
  - WSPOL-02
must_haves:
  truths:
    - "starvation と old-age の生存進行が behavior gate から外れ、monster lifecycle 側の survival 文脈で読める。"
    - "hunger migration は pure policy で migrant を選び、stage/application 側が destination と transition を適用する。"
    - "Phase 11 の facade / stage boundaries と tick order は維持され、monster tick の外部契約が変わらない。"
  artifacts:
    - path: "src/ai_rpg_world/application/world/services/monster_lifecycle_survival_coordinator.py"
      provides: "starvation / old-age / hunger migration apply をまとめる survival collaborator"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py"
      provides: "spawn/respawn と survival の 2 つの読み口を持つ lifecycle stage"
    - path: "tests/application/world/services/test_monster_lifecycle_survival_coordinator.py"
      provides: "survival coordinator の unit regression"
    - path: "tests/application/world/services/test_hunger_migration.py"
      provides: "facts -> policy -> apply の integration regression"
  key_links:
    - from: "src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/monster_lifecycle_survival_coordinator.py"
      via: "spawn/respawn 後の survival progression"
      pattern: "survival"
    - from: "src/ai_rpg_world/application/world/services/monster_lifecycle_survival_coordinator.py"
      to: "src/ai_rpg_world/application/world/services/hunger_migration_policy.py"
      via: "facts から migrant を選び apply へ渡す"
      pattern: "HungerMigrationPolicy"
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      via: "lifecycle 後に behavior が走る既存 tick order"
      pattern: "_monster_lifecycle_stage|_monster_behavior_stage"
---

<objective>
monster lifecycle の survival 境界を明示し、Phase 11 facade と tick order を維持したまま hunger migration の適用 orchestration を pure policy の外側へ押し出す。

Purpose: WSPOL-01 を facts -> policy -> apply の形で完結させ、WSPOL-02 の lifecycle / behavior 境界をコード上で判別できる状態にする。
Output: survival coordinator を持つ lifecycle stage、policy 利用の hunger migration orchestration、lifecycle 回帰テスト。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-monster-policy-separation/12-CONTEXT.md
@.planning/phases/12-monster-policy-separation/12-RESEARCH.md
@.planning/phases/12-monster-policy-separation/12-VALIDATION.md
@.planning/phases/12-monster-policy-separation/12-01-PLAN.md
@src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py
@src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@tests/application/world/services/test_hunger_migration.py
@tests/application/world/services/test_world_simulation_service.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py:
```python
class WorldSimulationMonsterLifecycleStageService:
    def run(
        self,
        maps: List[PhysicalMapAggregate],
        active_spot_ids: Set[SpotId],
        current_tick: WorldTick,
    ) -> None: ...
```

Current lifecycle order in src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
self._monster_lifecycle_stage.run(maps, active_spot_ids, current_tick)
self._monster_behavior_stage.run(maps, active_spot_ids, current_tick)
self._hit_box_stage.run(maps, active_spot_ids, current_tick)
```

Current hunger migration apply seam in src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
connected = self._connected_spots_provider.get_connected_spots(physical_map.spot_id)
to_spot_id = random.choice(connected)
self._map_transition_service.transition_object(...)
self._monster_repository.save(migrant)
```

Regression anchors:
```python
def test_hunger_migration_moves_only_the_hungriest_monster_per_tick(...): ...
def test_dead_monster_respawns_when_interval_elapsed_and_condition_met(...): ...
def test_only_active_spot_gets_build_observation_and_save(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lifecycle survival coordinator の unit anchor を追加する</name>
  <files>tests/application/world/services/test_monster_lifecycle_survival_coordinator.py</files>
  <behavior>
    - Test 1: starvation と old-age が behavior stage ではなく survival coordinator で処理される。
    - Test 2: survival coordinator は actor を actable/unactable に振り分け、死亡した actor を behavior 側へ流さない。
    - Test 3: hunger migration apply は policy が返した migrant のみを対象にし、destination 選定は policy に含めない。
  </behavior>
  <action>`test_monster_lifecycle_survival_coordinator.py` を新規作成し、Plan 02 の boundary を先に固定する。test では starvation / old-age / migration apply を同じ survival 文脈で扱えることを示しつつ、spawn/respawn 本体の詳細には広げすぎない。behavior stage に残すのは busy/active-time gate だけであることが読める命名にする。destination selection 改善や新 capability は期待値に含めない。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_monster_lifecycle_survival_coordinator.py -x</automated>
  </verify>
  <done>lifecycle survival 境界を表す unit regression file が存在し、Plan 02 の実装を先に拘束している。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lifecycle stage を spawn/respawn と survival の 2 つの読み口へ整理する</name>
  <files>src/ai_rpg_world/application/world/services/monster_lifecycle_survival_coordinator.py, src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_service.py, tests/application/world/services/test_monster_lifecycle_survival_coordinator.py, tests/application/world/services/test_hunger_migration.py</files>
  <behavior>
    - Test 1: starvation / old-age は lifecycle survival coordinator で save + sync event され、behavior stage の `_can_actor_act()` に残らない。
    - Test 2: hunger migration は candidates facts を policy に渡して migrant を選び、connected spot / gateway / transition は apply 側が担当する。
    - Test 3: spawn と respawn は同じ lifecycle stage から見えるまま維持される。
  </behavior>
  <action>`monster_lifecycle_survival_coordinator.py` を追加し、starvation、old-age、hunger migration apply を lifecycle 文脈にまとめる。`WorldSimulationMonsterLifecycleStageService` は `spawn/respawn` と `survival` の 2 つの読み口で上から読める形に整理し、Phase 11 の stage entry は維持すること。`WorldSimulationMonsterBehaviorStageService._can_actor_act()` から starvation / old-age の save/event 処理を外し、busy と active-time 중심の gate に戻す。hunger migration apply は Plan 01 の `HungerMigrationPolicy` を使って migrant selection だけ pure にし、connected spots / random destination / gateway / transition / repository save は coordinator 側に残す。tick order、active spot 限定、facade wiring は変えない。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_monster_lifecycle_survival_coordinator.py tests/application/world/services/test_hunger_migration.py -x</automated>
  </verify>
  <done>lifecycle stage が spawn/respawn と survival の 2 文脈で読め、starvation / old-age / hunger migration apply が behavior stage から外れている。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: world simulation regression で facade 境界と tick order 維持を固定する</name>
  <files>tests/application/world/services/test_world_simulation_service.py, src/ai_rpg_world/application/world/services/world_simulation_service.py</files>
  <behavior>
    - Test 1: lifecycle stage のあとに behavior stage が走る Phase 11 の順序契約が維持される。
    - Test 2: active spot 限定の behavior/update/save 契約は分離後も維持される。
    - Test 3: respawn と monster pursuit regression は facade 経由で継続し、外部契約が変わらない。
  </behavior>
  <action>既存 `test_world_simulation_service.py` を最小限更新し、Phase 12 で変える boundary だけを回帰で固定する。新しい stage 単体テスト群へ広げるのではなく、facade から見える tick order と monster regression slice を残すことを優先する。Phase 11 facade / stage boundaries、post-tick hook、ToolArgumentResolver 非依存といった既存契約は崩さない。world simulation の外部 API 変更はしない。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_world_simulation_service.py -k "dead_monster_respawns_when_interval_elapsed_and_condition_met or only_active_spot_gets_build_observation_and_save or monster" -x</automated>
  </verify>
  <done>Phase 12 の分離後も world simulation facade が既存 tick order と monster regression 契約を維持していることが既存 test slice で示される。</done>
</task>

</tasks>

<verification>
Plan 02 完了時点で、monster lifecycle は spawn/respawn と survival の 2 文脈で読め、hunger migration は facts -> policy -> apply で分割され、それでも Phase 11 facade の tick order と monster regression slice が green のままであることを確認する。
</verification>

<success_criteria>
starvation / old-age / hunger migration apply が lifecycle 側へ移り、monster behavior は行動判定専用の一本道 coordinator として残る。`WorldSimulationService` facade、stage 順序、既存 monster tick の外部契約は変わらない。
</success_criteria>

<output>
After completion, create `.planning/phases/12-monster-policy-separation/12-monster-policy-separation-02-SUMMARY.md`
</output>
