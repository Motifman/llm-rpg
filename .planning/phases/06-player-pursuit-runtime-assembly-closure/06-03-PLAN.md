---
phase: 06-player-pursuit-runtime-assembly-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/application/llm/test_llm_wiring_integration.py
  - tests/application/llm/test_tool_command_mapper.py
  - tests/application/observation/test_observation_event_handler.py
  - tests/application/world/services/test_pursuit_command_service.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - PURS-03
  - PURS-04
  - PURS-05
  - RUNT-01
  - OBSV-02
user_setup: []
must_haves:
  truths:
    - The audit gap only closes durably if regression coverage proves the authoritative bootstrap path cannot silently lose pursuit support in future edits.
    - The final regression set must check command-path availability, runtime continuation availability, and observation-driven turn scheduling as one coherent contract.
    - The proofs must stay anchored to the non-test runtime bootstrap path created in Phase 6 rather than reverting to hand-assembled services in tests.
    - Existing pursuit semantics, movement interruption semantics, and observation formatting behavior must remain unchanged except for being reachable from live composition.
    - This plan is the anti-regression lock: if any of the three seams drift apart again, at least one Phase 6 test should fail immediately.
  artifacts:
    - path: tests/application/llm/test_llm_wiring_integration.py
      provides: Bootstrap-level regression suite for pursuit-capable runtime composition
      min_lines: 120
    - path: tests/application/llm/test_tool_command_mapper.py
      provides: Guardrails that pursuit tools remain available on the authoritative runtime path
      min_lines: 40
    - path: tests/application/observation/test_observation_event_handler.py
      provides: Regression guard that pursuit outcomes still schedule turns without collapsing into movement interruption semantics
      min_lines: 60
  key_links:
    - from: tests/application/llm/test_tool_command_mapper.py
      to: src/ai_rpg_world/application/llm/wiring/__init__.py
      via: Tool-level regressions should fail if bootstrap composition stops exposing pursuit-capable tool mapping
      pattern: pursuit_enabled|pursuit_service
    - from: tests/application/llm/test_llm_wiring_integration.py
      to: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      via: Integration regressions must instantiate the authoritative runtime entrypoint directly
      pattern: player_pursuit_runtime|bootstrap
    - from: tests/application/observation/test_observation_event_handler.py
      to: src/ai_rpg_world/application/observation/handlers/observation_event_handler.py
      via: Observation regressions should preserve pursuit outcome scheduling semantics while Phase 6 changes runtime composition
      pattern: schedules_turn|pursuit
---

<objective>
Lock the runtime assembly fix in place with anti-regression coverage.

Purpose: Prevent the repository from drifting back to a half-wired pursuit runtime after Phase 6 lands.
Output: A final regression suite that treats pursuit-capable runtime composition as an explicit shipped contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-RESEARCH.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-VALIDATION.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-01-PLAN.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-02-PLAN.md
@src/ai_rpg_world/presentation/player_pursuit_runtime.py
@src/ai_rpg_world/application/llm/wiring/__init__.py
@tests/application/llm/test_llm_wiring_integration.py
@tests/application/llm/test_tool_command_mapper.py
@tests/application/observation/test_observation_event_handler.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add bootstrap regressions that fail on partial pursuit wiring</name>
  <files>tests/application/llm/test_llm_wiring_integration.py</files>
  <action>Add focused regression tests against the new runtime bootstrap that separately fail when command-path wiring, continuation wiring, or observation wiring is missing. The intent is to make future omissions obvious instead of allowing an apparently healthy runtime with degraded pursuit support.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "bootstrap and pursuit"</verify>
  <done>The bootstrap contract is guarded by tests that treat partial pursuit wiring as a regression, not as an optional variation.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve pursuit tool and observation guardrails while runtime composition changes</name>
  <files>tests/application/llm/test_tool_command_mapper.py, tests/application/observation/test_observation_event_handler.py</files>
  <action>Add or tighten tests confirming that pursuit start/cancel tool mapping remains available on the pursuit-capable runtime profile and that pursuit failed/cancelled observations still schedule turns without inheriting movement interruption semantics. Keep these tests focused on guardrails rather than re-proving the full integration path.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_tool_command_mapper.py tests/application/observation/test_observation_event_handler.py -q -k "pursuit"</verify>
  <done>Tool and observation guardrails fail if future runtime refactors accidentally disable pursuit commands or change pursuit outcome scheduling semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full Phase 6 regression slice and document the contract</name>
  <files>tests/application/llm/test_llm_wiring_integration.py, tests/application/llm/test_tool_command_mapper.py, tests/application/world/services/test_pursuit_command_service.py, tests/application/world/services/test_world_simulation_service.py, tests/application/observation/test_observation_event_handler.py</files>
  <action>Execute and stabilize the full Phase 6 regression slice described in `06-VALIDATION.md`. If any assertion currently relies on test-local hand wiring instead of the authoritative runtime path, adjust it so the final contract is centered on the shipped composition seam. Keep file edits minimal and targeted to regression clarity.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py tests/application/llm/test_tool_command_mapper.py tests/application/world/services/test_pursuit_command_service.py tests/application/world/services/test_world_simulation_service.py tests/application/observation/test_observation_event_handler.py -q</verify>
  <done>The final regression slice treats pursuit-capable runtime composition as a stable shipped behavior and catches future drift immediately.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py tests/application/llm/test_tool_command_mapper.py tests/application/world/services/test_pursuit_command_service.py tests/application/world/services/test_world_simulation_service.py tests/application/observation/test_observation_event_handler.py -q`
- [ ] At least one bootstrap-level test fails if pursuit command wiring is removed
- [ ] At least one bootstrap-level test fails if pursuit continuation wiring is removed
- [ ] Observation scheduling semantics for pursuit outcomes remain distinct from movement interruption semantics
- [ ] The final regression slice proves the authoritative runtime path, not a test-only equivalent
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 ends with regression coverage strong enough that the same runtime assembly gap cannot recur unnoticed
  </success_criteria>

<output>
After completion, create `.planning/phases/06-player-pursuit-runtime-assembly-closure/06-03-SUMMARY.md`
</output>
