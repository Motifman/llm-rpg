"""スポットグラフ文脈のアプリケーション層。

## アプリケーションサービスをここで再輸出しない理由

以前はここで 18 個のアプリケーションサービスと DTO を再輸出していた。**その
ためこのパッケージ配下の submodule を 1 つ import すると、それら 18 個とその
依存が全部読み込まれた。** Python はパッケージの ``__init__`` を先に実行するので、
小さなモジュールを 1 つ取るだけで実装まで付いてくる。

これが循環 import の火種になる (#1018 で ``llm`` 側が実際に発火した: 観測層の
型定義が ``llm.contracts.dtos`` を引いたら ``llm/__init__.py`` 経由で
``prompt_builder_config`` が読み込まれ、観測層へ戻ってきた)。

18 個すべて ``from ai_rpg_world.application.world_graph import ...`` の形では
**一度も使われていなかった** (AST で ``src/`` ``tests/`` ``scripts/`` の import 文を
数えて 0 件)。利用側はいずれも実体のモジュールを直接指している (合計 115 件)。
**火種だけを抱えて利点は無かった。**

**実装は各モジュールから直接 import する。**
"""
