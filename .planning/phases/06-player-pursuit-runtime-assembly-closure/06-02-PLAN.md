---
phase: 06-player-pursuit-runtime-assembly-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/ai_rpg_world/presentation/player_pursuit_runtime.py
  - tests/application/llm/test_llm_wiring_integration.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - PURS-03
  - PURS-04
  - OBSV-02
user_setup: []
must_haves:
  truths:
    - The authoritative runtime assembled in 06-01 must prove the full live path from pursuit start through world tick continuation into observation-driven turn resumption.
    - Phase 6 must reuse existing pursuit continuation and observation delivery behavior rather than inventing a second delivery pipeline or second tick loop.
    - The proof path must run through the assembled runtime entrypoint, not through test-local service construction that bypasses the new composition seam.
    - Pursuit-visible refresh and last-known continuation remain Phase 3 semantics; this plan proves they are reachable on the live runtime path.
    - Pursuit failed or cancelled outcomes must schedule LLM resumption through the existing observation handler and turn trigger path on the assembled runtime.
  artifacts:
    - path: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      provides: Runtime path that exposes assembled pursuit start, world tick continuation, and observation-triggered turn resumption together
      min_lines: 100
    - path: tests/application/llm/test_llm_wiring_integration.py
      provides: Integration proof that assembled pursuit events schedule and drain turns through the runtime path
      min_lines: 100
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: Regression coverage that the assembled runtime still delegates continuation through the existing world-tick seam
      min_lines: 80
  key_links:
    - from: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      to: src/ai_rpg_world/application/world/services/world_simulation_service.py
      via: The assembled runtime must drive pursuit continuation through the existing world tick rather than a separate scheduler
      pattern: tick|pursuit_continuation_service
    - from: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      to: src/ai_rpg_world/infrastructure/events/event_handler_composition.py
      via: Pursuit outcome events must enter the same observation registry used for turn scheduling
      pattern: observation_registry|register_for_profile
    - from: tests/application/llm/test_llm_wiring_integration.py
      to: src/ai_rpg_world/application/observation/handlers/observation_event_handler.py
      via: Integration proof should demonstrate that pursuit outcomes schedule turns through the existing observation handler path
      pattern: schedules_turn|schedule_turn
---

<objective>
Prove the assembled runtime supports the full player pursuit execution path.

Purpose: Demonstrate that pursuit start on the live runtime leads to tick continuation and that pursuit outcomes resume LLM turns through the existing observation flow.
Output: Integration coverage around the assembled runtime path plus targeted runtime support needed to expose and observe the end-to-end behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-RESEARCH.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-VALIDATION.md
@.planning/phases/04-observation-and-llm-delivery/04-01-PLAN.md
@.planning/phases/04-observation-and-llm-delivery/04-02-PLAN.md
@src/ai_rpg_world/presentation/player_pursuit_runtime.py
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/application/observation/handlers/observation_event_handler.py
@src/ai_rpg_world/application/observation/services/observation_formatter.py
@tests/application/llm/test_llm_wiring_integration.py
@tests/application/world/services/test_world_simulation_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expose the assembled player pursuit runtime for end-to-end integration tests</name>
  <files>src/ai_rpg_world/presentation/player_pursuit_runtime.py, tests/application/llm/test_llm_wiring_integration.py</files>
  <action>Extend the authoritative runtime bootstrap so tests and callers can obtain the assembled pieces needed to exercise pursuit start, world tick, and observation-driven resumption on one live path. Keep the API narrow and explicit; returning the composed service, trigger, and composition handles is acceptable if it reflects the real runtime boundary.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "bootstrap and pursuit"</verify>
  <done>The assembled runtime entrypoint can be used as the single source for live-path integration tests without hand-wiring services inside test bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Add start-to-tick continuation proof on the assembled runtime path</name>
  <files>tests/application/llm/test_llm_wiring_integration.py, tests/application/world/services/test_world_simulation_service.py</files>
  <action>Add integration coverage showing that a pursuit started through the assembled runtime's live tool/service path survives into `WorldSimulationApplicationService.tick()` and uses the existing continuation seam for visible refresh and last-known behavior. Make the assertions explicit enough that a missing continuation injection or bypassed bootstrap would fail clearly.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py tests/application/llm/test_llm_wiring_integration.py -q -k "pursuit and tick"</verify>
  <done>The assembled runtime proves the player pursuit start path reaches the world-tick continuation path in non-test composition.</done>
</task>

<task type="auto">
  <name>Task 3: Add pursuit outcome to LLM turn-resume proof on the assembled runtime path</name>
  <files>tests/application/llm/test_llm_wiring_integration.py</files>
  <action>Add or adapt an integration test so a pursuit failed or cancelled outcome produced on the assembled runtime path passes through the observation handler, schedules a turn, and is drained by `run_scheduled_turns()` on world tick. Reuse the current observation formatter/handler semantics; do not introduce new resumption plumbing.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "pursuit and scheduled"</verify>
  <done>The live runtime path proves `OBSV-02` by showing pursuit outcomes schedule and drain turns through the existing observation contract.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or bootstrap or scheduled"`
- [ ] Pursuit start on the assembled runtime reaches `WorldSimulationApplicationService.tick()`
- [ ] Tick continuation still uses the existing continuation seam rather than a parallel loop
- [ ] Pursuit failed/cancelled outcomes schedule and drain LLM turns on the same assembled runtime path
- [ ] No new observation formatter schema or turn-trigger mechanism is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The authoritative runtime path proves the audit's broken player pursuit flow is now executable from start through observation-driven turn resumption
  </success_criteria>

<output>
After completion, create `.planning/phases/06-player-pursuit-runtime-assembly-closure/06-02-SUMMARY.md`
</output>
