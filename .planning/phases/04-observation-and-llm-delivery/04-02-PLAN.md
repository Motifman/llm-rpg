---
phase: 04-observation-and-llm-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/observation/services/observation_formatter.py
  - src/ai_rpg_world/application/observation/contracts/dtos.py
  - tests/application/observation/test_observation_formatter.py
autonomous: true
requirements:
  - OUTC-03
  - RUNT-03
  - OBSV-01
  - OBSV-02
user_setup: []
must_haves:
  truths:
    - `PursuitFailedEvent` observations preserve machine-readable `failure_reason` from the pursuit enum so LLM and memory layers can branch without parsing prose.
    - Observation payloads explicitly distinguish movement interruption semantics from pursuit lifecycle outcomes using stable structured keys.
    - Pursuit fail/cancel outcomes schedule LLM turns through existing observation output flags, while avoiding movement-break side effects for pure pursuit outcomes.
    - Formatter output remains dual-channel: readable prose for prompts and structured payload for downstream extraction.
  artifacts:
    - path: src/ai_rpg_world/application/observation/services/observation_formatter.py
      provides: Pursuit event formatting for prose, structured payload, and scheduling flags
      min_lines: 220
    - path: tests/application/observation/test_observation_formatter.py
      provides: Regression coverage for pursuit structured fields and schedules_turn policy
      min_lines: 140
    - path: src/ai_rpg_world/application/observation/contracts/dtos.py
      provides: Observation output contract updates only if needed for explicit interruption semantics
      contains: ObservationOutput
  key_links:
    - from: src/ai_rpg_world/application/observation/services/observation_formatter.py
      to: src/ai_rpg_world/domain/pursuit/event/pursuit_events.py
      via: Formatter maps pursuit lifecycle events to observation prose and structured payloads
      pattern: PursuitFailedEvent|PursuitCancelledEvent
    - from: src/ai_rpg_world/application/observation/services/observation_formatter.py
      to: src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
      via: Failure reason is exported as stable enum-backed string value in structured output
      pattern: failure_reason|value
    - from: tests/application/observation/test_observation_formatter.py
      to: src/ai_rpg_world/application/observation/services/observation_formatter.py
      via: Tests enforce scheduling and interruption-scope semantics for pursuit outputs
      pattern: schedules_turn|breaks_movement|interruption_scope
---

<objective>
Implement pursuit observation formatting that carries actionable outcome semantics into LLM-facing output.

Purpose: Satisfy `OUTC-03`, `RUNT-03`, and `OBSV-02` by emitting pursuit observation payloads with explicit failure reasons, interruption scope, and turn scheduling behavior.
Output: Pursuit-aware formatter behavior and tests that lock structured payload fidelity and scheduling flags.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-observation-and-llm-delivery/04-RESEARCH.md
@.planning/phases/04-observation-and-llm-delivery/04-VALIDATION.md
@src/ai_rpg_world/application/observation/services/observation_formatter.py
@src/ai_rpg_world/application/observation/contracts/dtos.py
@src/ai_rpg_world/domain/pursuit/event/pursuit_events.py
@src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
@src/ai_rpg_world/application/llm/services/recent_events_formatter.py
@src/ai_rpg_world/application/llm/services/memory_extractor.py
@tests/application/observation/test_observation_formatter.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add pursuit lifecycle formatting paths in ObservationFormatter</name>
  <files>src/ai_rpg_world/application/observation/services/observation_formatter.py</files>
  <action>Extend formatter dispatch to support `PursuitStartedEvent`, `PursuitUpdatedEvent`, `PursuitFailedEvent`, and `PursuitCancelledEvent`. For each, emit concise prose and structured payload keys that make pursuit state transitions explicit. Keep payload naming stable and v1-focused: include `event_type`, `actor_id`, `target_id`, `pursuit_status_after_event`, and `interruption_scope` where appropriate.</action>
  <verify>.venv/bin/python -m pytest tests/application/observation/test_observation_formatter.py -q -k "pursuit and (started or updated or failed or cancelled)"</verify>
  <done>ObservationFormatter consistently returns pursuit observation outputs for pursuit lifecycle events.</done>
</task>

<task type="auto">
  <name>Task 2: Encode failure reason fidelity and interruption distinction in structured output</name>
  <files>src/ai_rpg_world/application/observation/services/observation_formatter.py, src/ai_rpg_world/application/observation/contracts/dtos.py</files>
  <action>Ensure pursuit failed output includes `failure_reason` from `PursuitFailureReason.value`, plus relevant `last_known` and optional snapshot metadata. Encode explicit runtime distinction fields for `RUNT-03` such as `interruption_scope` (`movement` vs `pursuit`) and `pursuit_status_after_event` (`active` vs `ended`). Update DTO contract only if existing typing blocks these fields; avoid unnecessary contract expansion.</action>
  <verify>.venv/bin/python -m pytest tests/application/observation/test_observation_formatter.py -q -k "failure_reason or interruption_scope or pursuit_status_after_event"</verify>
  <done>Pursuit observations expose machine-readable failure and interruption semantics without relying on prose parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Set turn scheduling policy for pursuit outcomes and lock tests</name>
  <files>src/ai_rpg_world/application/observation/services/observation_formatter.py, tests/application/observation/test_observation_formatter.py</files>
  <action>Set `schedules_turn=True` for pursuit failed/cancelled outcomes so the existing observation handler can trigger LLM turn resumption. Keep `breaks_movement=False` for pursuit outcome events unless a movement event itself is being represented. Add tests that pin this policy and protect against regressions where pursuit outcomes stop scheduling turns.</action>
  <verify>.venv/bin/python -m pytest tests/application/observation/test_observation_formatter.py -q -k "schedules_turn or breaks_movement or pursuit"</verify>
  <done>Formatter emits pursuit outcome observations that correctly trigger downstream LLM scheduling behavior.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/observation/test_observation_formatter.py -q -k "pursuit or failure_reason or schedules_turn"`
- [ ] Pursuit failed output includes enum-backed `failure_reason` in structured payload
- [ ] Structured payload explicitly distinguishes interruption scope and pursuit post-event status
- [ ] Pursuit failed/cancelled outputs set `schedules_turn=True`
- [ ] Pursuit outcome outputs do not mark `breaks_movement=True` by default
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Pursuit outcome observations become LLM-actionable via explicit structured semantics and existing scheduling mechanics
  </success_criteria>

<output>
After completion, create `.planning/phases/04-observation-and-llm-delivery/04-02-SUMMARY.md`
</output>
