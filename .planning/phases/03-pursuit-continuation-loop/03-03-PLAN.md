---
phase: 03-pursuit-continuation-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/application/world/services/test_world_simulation_service.py
  - tests/application/world/services/test_movement_service.py
  - tests/application/world/services/test_pursuit_command_service.py
  - tests/domain/player/aggregate/test_player_status_aggregate.py
autonomous: true
requirements:
  - PURS-03
  - PURS-04
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - Visible-target pursuit refresh continues movement through the world tick without regressing ordinary non-pursuit movement behavior.
    - Lost-visibility pursuit continues toward frozen `last_known` and fails with `vision_lost_at_last_known` only after arrival without reacquiring the target.
    - Continuation distinguishes pathless pursuit recovery, unreachable replanning, and truly missing targets with regressions for each outcome.
    - Existing Phase 2 explicit start/cancel semantics remain intact after Phase 3 continuation wiring.
    - Unchanged pursuit refresh input remains a no-op that does not emit `PursuitUpdatedEvent` every tick.
  artifacts:
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: End-to-end runtime regression coverage for pursuit continuation through world tick
      min_lines: 160
    - path: tests/application/world/services/test_movement_service.py
      provides: Path refresh and failure-mode regression coverage for pursuit-continuation integration
      min_lines: 80
    - path: tests/application/world/services/test_pursuit_command_service.py
      provides: Guardrail coverage that Phase 2 pursuit command semantics survive Phase 3 continuation changes
      min_lines: 40
    - path: tests/domain/player/aggregate/test_player_status_aggregate.py
      provides: Aggregate-level confirmation that pursuit update events remain no-op on unchanged state
      min_lines: 40
  key_links:
    - from: tests/application/world/services/test_world_simulation_service.py
      to: src/ai_rpg_world/application/world/services/world_simulation_service.py
      via: Regression tests prove pursuit continuation remains in the correct tick stage
      pattern: pursuit|tick
    - from: tests/application/world/services/test_movement_service.py
      to: src/ai_rpg_world/application/world/services/movement_service.py
      via: Regression tests pin the movement-layer contract used by pursuit continuation
      pattern: path|destination|tick_movement
    - from: tests/domain/player/aggregate/test_player_status_aggregate.py
      to: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      via: Aggregate tests guarantee meaningful-change gating for pursuit updates
      pattern: update_pursuit|PursuitUpdatedEvent
---

<objective>
Finish Phase 3 with regression coverage that proves the continuation loop is stable across runtime, movement, and aggregate layers.

Purpose: Convert the Phase 3 implementation into a safely executable and reviewable slice by locking in the new behaviors and guarding against regressions in earlier pursuit command work.
Output: A focused regression suite covering visible pursuit continuation, frozen last-known behavior, structured failures, and unchanged Phase 2 semantics.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-pursuit-continuation-loop/03-CONTEXT.md
@.planning/phases/03-pursuit-continuation-loop/03-RESEARCH.md
@.planning/phases/03-pursuit-continuation-loop/03-VALIDATION.md
@.planning/phases/03-pursuit-continuation-loop/03-01-PLAN.md
@.planning/phases/03-pursuit-continuation-loop/03-02-PLAN.md
@tests/application/world/services/test_world_simulation_service.py
@tests/application/world/services/test_movement_service.py
@tests/application/world/services/test_pursuit_command_service.py
@tests/domain/player/aggregate/test_player_status_aggregate.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand world-tick integration coverage for the full continuation loop</name>
  <files>tests/application/world/services/test_world_simulation_service.py</files>
  <action>Add or refine integration scenarios that cover visible-target movement causing pursuit refresh and same-tick movement, target loss with frozen last-known continuation, busy-tick skip preservation, pathless pursuit immediate replan, and last-known arrival causing `vision_lost_at_last_known`. Keep the assertions readable enough that the intended tick sequence is obvious from the test names and failure messages.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or last_known or busy"</verify>
  <done>The world-simulation suite captures the full Phase 3 continuation loop and its edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for movement and aggregate contracts Phase 3 depends on</name>
  <files>tests/application/world/services/test_movement_service.py, tests/domain/player/aggregate/test_player_status_aggregate.py</files>
  <action>Add targeted regressions for pursuit-driven path refresh behavior, empty-path handling after movement-side path clearing, and aggregate no-op semantics when pursuit refresh receives unchanged target information. Use aggregate tests to pin meaningful-change gating for `update_pursuit(...)` so the runtime continuation loop cannot regress into per-tick update-event spam.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_movement_service.py tests/domain/player/aggregate/test_player_status_aggregate.py -q -k "pursuit or movement"</verify>
  <done>Phase 3 depends on explicit test coverage for both movement integration and aggregate event-gating behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Guard against regressions in explicit pursuit command semantics</name>
  <files>tests/application/world/services/test_pursuit_command_service.py</files>
  <action>Add or update a small set of guardrail tests proving that Phase 2 explicit start/cancel semantics still hold after continuation-loop changes. Cover at least one successful start path, one same-target refresh/no-op path, and one explicit cancel path so continuation work cannot accidentally absorb responsibilities that still belong to command entrypoints.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_pursuit_command_service.py -q -k "pursuit and (start or cancel or refresh)"</verify>
  <done>The Phase 3 regression suite preserves the boundary between explicit pursuit commands and tick-driven continuation.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py tests/application/world/services/test_movement_service.py tests/application/world/services/test_pursuit_command_service.py tests/domain/player/aggregate/test_player_status_aggregate.py -q`
- [ ] World-tick tests cover visible refresh, frozen `last_known`, busy skip, pathless pursuit replan, and `vision_lost_at_last_known`
- [ ] Movement-layer tests distinguish empty-path recovery from unreachable-path failure
- [ ] Aggregate tests confirm unchanged pursuit refresh does not emit a meaningful update
- [ ] Phase 2 explicit start/cancel behavior still passes after Phase 3 continuation wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 ends with regression coverage strong enough for downstream execution and later Phase 4 work without reopening the runtime continuation contract
  </success_criteria>

<output>
After completion, create `.planning/phases/03-pursuit-continuation-loop/03-03-SUMMARY.md`
</output>
