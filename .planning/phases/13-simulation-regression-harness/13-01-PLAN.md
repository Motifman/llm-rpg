---
phase: 13-simulation-regression-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/application/world/services/test_world_simulation_service.py
  - tests/application/world/services/test_world_simulation_stage_order.py
  - tests/application/world/services/test_world_simulation_active_spot_contract.py
  - tests/application/world/services/test_world_simulation_fixtures.py
  - tests/application/world/services/test_world_simulation_post_tick_hooks.py
autonomous: true
requirements:
  - WSTEST-01
  - WSTEST-02
must_haves:
  truths:
    - "WorldSimulationApplicationService の代表的な facade regression が、stage order・active spot/save・post-tick hook の契約ごとに読める。"
    - "代表ケースは『プレイヤー1体 + pursuit / movement + モンスター1体』を中心にし、world simulation 本流のつながりを壊さずに守る。"
    - "新しい integration regression は、全部入り `setup_service` へ依存しなくても追加できる薄い builder / helper を持つ。"
  artifacts:
    - path: "tests/application/world/services/test_world_simulation_stage_order.py"
      provides: "tick 全体の stage 順序を守る契約別 integration"
    - path: "tests/application/world/services/test_world_simulation_active_spot_contract.py"
      provides: "active spot / save 境界を守る契約別 integration"
    - path: "tests/application/world/services/test_world_simulation_post_tick_hooks.py"
      provides: "llm_turn_trigger / reflection_runner を守る post-tick hook integration"
    - path: "tests/application/world/services/test_world_simulation_fixtures.py"
      provides: "representative world を軽く組み立てる builder / helper"
  key_links:
    - from: "tests/application/world/services/test_world_simulation_stage_order.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      via: "environment -> movement -> harvest -> lifecycle -> behavior -> hit box -> post-tick hook の順序保証"
      pattern: "_environment_stage|_movement_stage|_harvest_stage|_monster_lifecycle_stage|_monster_behavior_stage|_hit_box_stage"
    - from: "tests/application/world/services/test_world_simulation_active_spot_contract.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_hit_box_stage_service.py"
      via: "active_spot_ids と save 境界"
      pattern: "active_spot_ids|save"
    - from: "tests/application/world/services/test_world_simulation_post_tick_hooks.py"
      to: "tests/application/llm/test_llm_wiring_integration.py"
      via: "facade 内 post-tick 契約と wiring 契約の整合"
      pattern: "llm_turn_trigger|reflection_runner"
---

<objective>
Phase 13 の最初の計画として、world simulation facade regression を「どの契約を守るテストか」が読める形に切り分け、代表ケースと軽量 builder / helper を整える。

Purpose: WSTEST-01 を先に安定させ、Phase 12 までで作られた seam を使って巨大 integration file の責務集中を和らげる。
Output: 契約別 integration test files、representative world 用 helper、post-tick hook を明示する regression。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-simulation-regression-harness/13-CONTEXT.md
@.planning/phases/13-simulation-regression-harness/13-RESEARCH.md
@.planning/phases/12-monster-policy-separation/12-monster-policy-separation-02-SUMMARY.md
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/application/world/services/world_simulation_hit_box_stage_service.py
@tests/application/world/services/test_world_simulation_service.py
@tests/application/llm/test_llm_wiring_integration.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
class WorldSimulationApplicationService:
    def tick(self) -> WorldTick: ...
    def _tick_impl(self) -> WorldTick: ...
    def _run_post_tick_hooks(self, current_tick: WorldTick) -> None: ...
```

Facade order contract in `_tick_impl()`:
```python
self._environment_stage.run(current_tick, maps)
self._movement_stage.run(current_tick)
self._harvest_stage.run(maps, current_tick)
blocked_actor_ids = self._monster_lifecycle_stage.run(...)
self._monster_behavior_stage.run(..., skipped_actor_ids=blocked_actor_ids)
self._hit_box_stage.run(maps, active_spot_ids, current_tick)
```

Regression anchors in tests/application/world/services/test_world_simulation_service.py:
```python
def test_tick_runs_pursuit_continuation_before_movement_execution(...): ...
def test_tick_runs_monster_lifecycle_before_behavior_stage(...): ...
def test_only_active_spot_gets_build_observation_and_save(...): ...
def test_tick_calls_llm_turn_trigger_run_scheduled_turns_when_provided(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: representative world を組む軽量 builder / helper を抽出する</name>
  <files>tests/application/world/services/test_world_simulation_fixtures.py, tests/application/world/services/test_world_simulation_service.py</files>
  <behavior>
    - Test 1: 新しい helper で「プレイヤー1体 + pursuit / movement + モンスター1体」の代表 world を短く組める。
    - Test 2: helper 名と返り値だけで、どの前提 world を作るか読める。
    - Test 3: 既存 `setup_service` をすぐ消さず、代表 regression の移行足場として共存できる。
  </behavior>
  <action>Phase 13 の新規 integration 追加で `setup_service` を直接複製しないよう、world simulation regression 専用の薄い helper / builder module を作る。helper は "何の契約を見る world か" を名前で表し、pathfinding・monster repository・skill loadout・hook mocks など必要なものだけを明示的に返すこと。共通化を急ぎすぎて汎用 fixture の箱を作るのではなく、stage order / active spot / post-tick hook を支える最小限の helper から始めること。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_service.py -k "pursuit_continuation_before_movement_execution or tick_runs_monster_lifecycle_before_behavior_stage" -x</automated>
  </verify>
  <done>representative integration を追加・移設するための軽量 helper が存在し、既存 facade regression の移行足場として使える。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: facade regression を契約別 integration へ切り分ける</name>
  <files>tests/application/world/services/test_world_simulation_stage_order.py, tests/application/world/services/test_world_simulation_active_spot_contract.py, tests/application/world/services/test_world_simulation_service.py</files>
  <behavior>
    - Test 1: representative world で `environment -> movement -> harvest -> lifecycle -> behavior -> hit box` の順序が守られる。
    - Test 2: active spot を持つ map だけが behavior / save の対象になる。
    - Test 3: 契約別 file に分けても、既存 facade regression の意味が失われない。
  </behavior>
  <action>既存 `test_world_simulation_service.py` から、Phase 13 で最優先の契約に当たる order / active spot regression を契約別 test file へ移す。1 テスト 1 契約を守り、"どの class を叩くか" ではなく "どの約束を守るか" が名前から分かるようにすること。既存 file は空洞化しすぎず、代表ケースや legacy coverage の起点として最小限残してよい。HitBox の細かい枝分かれ全面整理には踏み込まず、active spot/save 契約と facade 順序に必要な範囲で止めること。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_stage_order.py tests/application/world/services/test_world_simulation_active_spot_contract.py -x</automated>
  </verify>
  <done>順序契約と active spot/save 契約が separate integration files で読める。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: post-tick hook regression を独立させて hook 契約を明示する</name>
  <files>tests/application/world/services/test_world_simulation_post_tick_hooks.py, tests/application/world/services/test_world_simulation_service.py</files>
  <behavior>
    - Test 1: `llm_turn_trigger.run_scheduled_turns()` が tick 本体の後に呼ばれる。
    - Test 2: `reflection_runner.run_after_tick()` が `llm_turn_trigger` と同じ post-tick 契約上で明示的に守られる。
    - Test 3: hook 側例外の facade error conversion 契約が既存期待値と両立する。
  </behavior>
  <action>Phase 11 research に残っていた `reflection_runner.run_after_tick()` の未明示ギャップを Phase 13 で回収する。`tests/application/llm/test_llm_wiring_integration.py` の wiring 契約と重複しすぎないよう、ここでは facade 内の post-tick contract に集中すること。`llm_turn_trigger` だけで満足せず、`reflection_runner` の呼び出し有無と順序も regression として固定する。hook 周りを新しい infra に載せ替えるのではなく、mock collaborator で contract を直接守ること。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_post_tick_hooks.py tests/application/llm/test_llm_wiring_integration.py -k "llm_turn_trigger or reflection_runner or WorldSimulationService" -x</automated>
  </verify>
  <done>post-tick hook 契約が facade regression と wiring integration の両輪で明示される。</done>
</task>

</tasks>

<verification>
Plan 13-01 完了時点で、world simulation facade regression が order / active spot / post-tick hook の契約ごとに分かれ、代表ケースを軽く組める helper を通じて追加しやすくなっていることを確認する。
</verification>

<success_criteria>
WSTEST-01 の中心契約が、巨大 file を読まなくても契約別 integration から把握できる。新しい representative regression を足すとき、全部入り `setup_service` を複製しなくて済む。
</success_criteria>

<output>
After completion, create `.planning/phases/13-simulation-regression-harness/13-simulation-regression-harness-01-SUMMARY.md`
</output>
