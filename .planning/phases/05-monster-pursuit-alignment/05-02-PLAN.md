---
phase: 05-monster-pursuit-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/ai_rpg_world/application/world/services/monster_action_resolver.py
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
  - tests/application/world/services/test_world_simulation_service.py
  - tests/domain/monster/aggregate/test_monster_aggregate.py
  - tests/domain/monster/service/test_behavior_state_transition_service.py
autonomous: true
requirements:
  - PURS-02
user_setup: []
must_haves:
  truths:
    - Monster pursuit does not search indefinitely after sight is lost; reaching last-known without reacquiring the target is treated as an explicit pursuit failure outcome.
    - That failure uses the shared pursuit reason vocabulary, especially `vision_lost_at_last_known`, so monster and player pursuit semantics stay basically aligned.
    - Reacquiring the same target during `SEARCH` continues the same pursuit context instead of discarding it and starting an unrelated path.
    - Exiting into `FLEE`, `RETURN`, or normal non-pursuit behavior after failure leaves no stale target or last-known state behind.
    - This plan locks the runtime behavior with regressions rather than widening Phase 5 into new observation or LLM delivery responsibilities.
  artifacts:
    - path: src/ai_rpg_world/application/world/services/monster_action_resolver.py
      provides: Search-movement seam that must stop treating exhausted last-known search as endless wandering
      min_lines: 40
    - path: src/ai_rpg_world/application/world/services/world_simulation_service.py
      provides: Runtime place where last-known exhaustion and target-missing outcomes can be finalized
      contains: _process_single_actor_behavior
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: Integration regressions for monster pursuit failure, reacquire, and normal-state return behavior
      min_lines: 120
    - path: tests/domain/monster/aggregate/test_monster_aggregate.py
      provides: Aggregate regression coverage for failure cleanup and pursuit state clearing
      min_lines: 60
  key_links:
    - from: src/ai_rpg_world/application/world/services/world_simulation_service.py
      to: src/ai_rpg_world/application/world/services/monster_action_resolver.py
      via: Runtime must distinguish a valid SEARCH move from exhausted last-known search that should fail pursuit
      pattern: SEARCH|last_known|move
    - from: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      to: src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
      via: Monster pursuit failure reasons should align with the shared pursuit outcome vocabulary
      pattern: VISION_LOST_AT_LAST_KNOWN|TARGET_MISSING
    - from: tests/application/world/services/test_world_simulation_service.py
      to: src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
      via: Integration tests prove that SEARCH reacquire and failure behavior still obey the domain transition rules
      pattern: lose_target|selected_target|search
---

<objective>
Complete Phase 5 by making monster last-known pursuit outcomes explicit and regression-safe.

Purpose: Ensure monster `SEARCH` behaves as real pursuit continuation rather than an open-ended local behavior, with explicit failure when last-known is exhausted and stable behavior when the target is reacquired.
Output: Runtime handling for monster pursuit failure/cleanup plus regressions that lock the full visible-chase -> search -> reacquire/fail lifecycle.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-monster-pursuit-alignment/05-CONTEXT.md
@.planning/phases/05-monster-pursuit-alignment/05-RESEARCH.md
@.planning/phases/05-monster-pursuit-alignment/05-VALIDATION.md
@.planning/phases/05-monster-pursuit-alignment/05-01-PLAN.md
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/application/world/services/monster_action_resolver.py
@src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
@src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
@src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
@tests/application/world/services/test_world_simulation_service.py
@tests/domain/monster/aggregate/test_monster_aggregate.py
@tests/domain/monster/service/test_behavior_state_transition_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Finalize last-known exhaustion as explicit monster pursuit failure</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_service.py, src/ai_rpg_world/application/world/services/monster_action_resolver.py, tests/application/world/services/test_world_simulation_service.py</files>
  <action>Add runtime logic that detects when a monster in `SEARCH` has reached its frozen last-known position and still has not reacquired the target, then ends the active monster pursuit with the shared `vision_lost_at_last_known` outcome instead of continuing indefinite search wandering. Keep the implementation inside the existing simulation/action seams; do not introduce a separate monster pursuit loop.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "last_known or vision_lost or monster pursuit"</verify>
  <done>Monster pursuit now fails explicitly when search is exhausted at the last-known location.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve same-target continuation when SEARCH reacquires visibility</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_service.py, src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py, tests/application/world/services/test_world_simulation_service.py, tests/domain/monster/aggregate/test_monster_aggregate.py</files>
  <action>Add or tighten the runtime and aggregate behavior so that if the same target becomes visible again during `SEARCH`, the monster resumes active chase on the same pursuit context rather than discarding target identity and constructing an unrelated state. Assert the shared target and last-known semantics stay continuous across `CHASE` -> `SEARCH` -> `CHASE`.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py tests/domain/monster/aggregate/test_monster_aggregate.py -q -k "reacquire or search or chase"</verify>
  <done>Reacquiring a target during SEARCH resumes the same pursuit context and stays aligned with Phase 5 decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage for cleanup and non-pursuit exits</name>
  <files>tests/application/world/services/test_world_simulation_service.py, tests/domain/monster/aggregate/test_monster_aggregate.py, tests/domain/monster/service/test_behavior_state_transition_service.py</files>
  <action>Add regression coverage for three cleanup boundaries: target missing, vision lost at last-known, and state exits into `FLEE` / `RETURN`. Each test should assert that aligned pursuit state, target identity, and last-known data are either preserved correctly for active pursuit or cleared correctly when pursuit has ended. Keep assertions focused on runtime behavior and shared failure vocabulary rather than observation delivery.</action>
  <verify>.venv/bin/python -m pytest tests/domain/monster/aggregate/test_monster_aggregate.py tests/domain/monster/service/test_behavior_state_transition_service.py tests/application/world/services/test_world_simulation_service.py -q -k "target_missing or vision_lost or flee or return or pursuit"</verify>
  <done>Phase 5 is protected by regressions that would fail if monster pursuit started wandering indefinitely, dropped the target too early, or leaked stale pursuit state after exit.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/domain/monster/aggregate/test_monster_aggregate.py tests/domain/monster/service/test_behavior_state_transition_service.py tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or last_known or vision_lost or target_missing or search or chase"`
- [ ] Reaching last-known without reacquiring the target ends monster pursuit explicitly instead of continuing indefinite search
- [ ] Monster pursuit failure uses shared pursuit reason vocabulary, especially `vision_lost_at_last_known`
- [ ] Reacquiring the same target during `SEARCH` resumes the same pursuit context
- [ ] `FLEE`, `RETURN`, and other non-pursuit exits leave no stale active pursuit data behind
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 finishes with regression-safe monster pursuit lifecycle behavior that matches the shared pursuit model for target retention, last-known handling, and failure cleanup
  </success_criteria>

<output>
After completion, create `.planning/phases/05-monster-pursuit-alignment/05-02-SUMMARY.md`
</output>
