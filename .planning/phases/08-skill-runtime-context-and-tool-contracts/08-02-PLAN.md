---
phase: 08-skill-runtime-context-and-tool-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/ai_rpg_world/application/llm/services/ui_context_builder.py
  - src/ai_rpg_world/application/llm/services/tool_argument_resolver.py
  - tests/application/llm/test_ui_context_builder.py
  - tests/application/llm/test_tool_argument_resolver.py
autonomous: true
requirements:
  - SKRT-01
  - SKTL-02
user_setup: []
must_haves:
  truths:
    - The prompt-visible labels and `ToolRuntimeContextDto.targets` must be generated together so the LLM never sees a label family that cannot be resolved.
    - Equip follows the locked action-first model: one label for the skill candidate and one for the destination slot.
    - Proposal labels are single-source identifiers reused by both accept and reject actions.
    - Awakened action uses a single action label and carries no raw duration/cost parameters.
    - Resolver behavior must remain typed and central; executor layers should not parse labels or infer target kinds.
  artifacts:
    - path: src/ai_rpg_world/application/llm/services/ui_context_builder.py
      provides: Prompt sections and runtime target registration for the new skill label families
      min_lines: 120
    - path: src/ai_rpg_world/application/llm/services/tool_argument_resolver.py
      provides: Canonical resolution for `skill_equip` and shared proposal/awakened target payloads
      min_lines: 120
    - path: tests/application/llm/test_tool_argument_resolver.py
      provides: Guardrails for invalid kind rejection and action-first equip resolution
      min_lines: 120
  key_links:
    - from: src/ai_rpg_world/application/llm/services/ui_context_builder.py
      to: src/ai_rpg_world/application/llm/contracts/dtos.py
      via: Builder output must instantiate the new typed runtime targets, not untyped dict payloads
      pattern: ToolRuntimeTargetDto|Skill
    - from: src/ai_rpg_world/application/llm/services/tool_argument_resolver.py
      to: src/ai_rpg_world/application/skill/contracts/commands.py
      via: Canonical outputs must match the argument shapes expected by downstream skill commands
      pattern: loadout_id|deck_tier|slot_index|skill_id|proposal_id
---

<objective>
Render and resolve the new skill label families through the existing LLM tooling path.

Purpose: expose the Phase 8 skill decision labels in prompt text and convert them back into canonical argument payloads without leaking raw internal IDs into the tool schema.
Output: UI context builder support plus resolver branches for equip, proposal, and awakened label families.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-skill-runtime-context-and-tool-contracts/08-CONTEXT.md
@.planning/phases/08-skill-runtime-context-and-tool-contracts/08-RESEARCH.md
@.planning/phases/08-skill-runtime-context-and-tool-contracts/08-VALIDATION.md
@.planning/phases/08-skill-runtime-context-and-tool-contracts/08-01-PLAN.md
@src/ai_rpg_world/application/llm/services/ui_context_builder.py
@src/ai_rpg_world/application/llm/services/tool_argument_resolver.py
@src/ai_rpg_world/application/skill/contracts/commands.py
@tests/application/llm/test_ui_context_builder.py
@tests/application/llm/test_tool_argument_resolver.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add prompt sections and labels for the new skill decision families</name>
  <files>src/ai_rpg_world/application/llm/services/ui_context_builder.py, tests/application/llm/test_ui_context_builder.py</files>
  <action>Extend the UI context builder to render sections for equip candidates, tiered slots, pending proposals, and awakened action. Keep ordering aligned with research guidance: after usable skills and before quest/guild/shop sections.</action>
  <verify>pytest tests/application/llm/test_ui_context_builder.py -x</verify>
  <done>Prompt text and runtime targets include each new skill label family with stable prefixes and coherent ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve action-first equip labels into canonical args</name>
  <files>src/ai_rpg_world/application/llm/services/tool_argument_resolver.py, tests/application/llm/test_tool_argument_resolver.py</files>
  <action>Add resolver support that combines equip candidate and equip slot labels into `loadout_id`, `deck_tier`, `slot_index`, and `skill_id`. Keep invalid-label and invalid-kind failures explicit.</action>
  <verify>pytest tests/application/llm/test_tool_argument_resolver.py -x -k \"equip or invalid\"</verify>
  <done>`skill_equip` can be driven by human-friendly label pairs and fails clearly on mismatched kinds or missing labels.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve proposal and awakened labels without leaking raw numeric inputs</name>
  <files>src/ai_rpg_world/application/llm/services/tool_argument_resolver.py, tests/application/llm/test_tool_argument_resolver.py</files>
  <action>Add resolver branches for proposal labels and awakened action labels so later tools can share the same target family. Preserve the locked rule that awakened activation exposes no numeric command parameters at the tool schema boundary.</action>
  <verify>pytest tests/application/llm/test_tool_argument_resolver.py -x -k \"proposal or awaken\"</verify>
  <done>The resolver can emit canonical payloads for proposal and awakened label families while keeping numeric policy server-owned.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/application/llm/test_ui_context_builder.py -x`
- [ ] `pytest tests/application/llm/test_tool_argument_resolver.py -x`
- [ ] Equip labels resolve as two coordinated labels, not a pre-composed pair
- [ ] Proposal labels can be reused by both accept and reject flows
- [ ] Awakened action resolution carries no raw duration/cost/cooldown numbers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 8 has prompt-visible skill label families and canonical resolution behavior aligned with the locked tool contract
  </success_criteria>

<output>
After completion, create `.planning/phases/08-skill-runtime-context-and-tool-contracts/08-02-SUMMARY.md`
</output>
