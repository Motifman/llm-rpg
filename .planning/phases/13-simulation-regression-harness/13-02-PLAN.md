---
phase: 13-simulation-regression-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/application/world/services/test_world_simulation_movement_stage_service.py
  - tests/application/world/services/test_world_simulation_monster_lifecycle_stage_service.py
  - tests/application/world/services/test_world_simulation_monster_behavior_stage_service.py
  - tests/application/world/services/test_hunger_migration.py
  - tests/application/world/services/test_monster_behavior_coordinator.py
autonomous: true
requirements:
  - WSTEST-02
must_haves:
  truths:
    - "movement / monster lifecycle / monster behavior の各 stage に、巨大 fixture へ依存しない guard regression が追加できる。"
    - "stage test は guard / skip / support 有無による collaborator 呼び出し契約に集中し、facade 順序契約を再実装しない。"
    - "既存の policy / coordinator 単体テスト文化と役割分担が明確で、何を stage で守り何を coordinator/policy で守るかが読める。"
  artifacts:
    - path: "tests/application/world/services/test_world_simulation_movement_stage_service.py"
      provides: "pursuit continuation / missing support / busy actor の movement stage regression"
    - path: "tests/application/world/services/test_world_simulation_monster_lifecycle_stage_service.py"
      provides: "spawn/respawn と survival handoff の lifecycle stage regression"
    - path: "tests/application/world/services/test_world_simulation_monster_behavior_stage_service.py"
      provides: "active spot / skipped actor / busy / active time の behavior stage regression"
  key_links:
    - from: "tests/application/world/services/test_world_simulation_movement_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py"
      via: "support 未注入、pursuit continuation、busy actor の guard 契約"
      pattern: "tick_movement_in_current_unit_of_work|evaluate_tick"
    - from: "tests/application/world/services/test_world_simulation_monster_lifecycle_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py"
      via: "spawn/respawn と survival coordinator の handoff"
      pattern: "process_spawn_and_respawn_by_slots|process_respawn_legacy|process_survival_for_spot"
    - from: "tests/application/world/services/test_world_simulation_monster_behavior_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      via: "skipped/busy/active-time guard と coordinator 委譲"
      pattern: "process_actor_behavior|is_active_at_time"
---

<objective>
Phase 13 の後半計画として、Phase 11/12 で導入した stage service seam に guard regression を追加し、巨大 fixture に依存しすぎない world simulation 回帰点を増やす。

Purpose: WSTEST-02 を達成し、今後の world simulation 分割で「どこを壊したか」を stage 境界で追えるようにする。
Output: movement / monster lifecycle / monster behavior stage の新規 test files と、既存 policy / coordinator test との役割整理。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-simulation-regression-harness/13-CONTEXT.md
@.planning/phases/13-simulation-regression-harness/13-RESEARCH.md
@.planning/phases/13-simulation-regression-harness/13-01-PLAN.md
@src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py
@src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py
@src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
@tests/application/world/services/test_hunger_migration.py
@tests/application/world/services/test_hunger_migration_policy.py
@tests/application/world/services/test_monster_behavior_coordinator.py
@tests/application/world/services/test_monster_lifecycle_survival_coordinator.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py:
```python
class WorldSimulationMovementStageService:
    def run(self, current_tick: WorldTick) -> None: ...
```

From src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py:
```python
class WorldSimulationMonsterLifecycleStageService:
    def run(
        self,
        maps: List[PhysicalMapAggregate],
        active_spot_ids: Set[SpotId],
        current_tick: WorldTick,
    ) -> Set[WorldObjectId]: ...
```

From src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py:
```python
class WorldSimulationMonsterBehaviorStageService:
    def run(
        self,
        maps: List[PhysicalMapAggregate],
        active_spot_ids: Set[SpotId],
        current_tick: WorldTick,
        skipped_actor_ids: Set[WorldObjectId] | None = None,
    ) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: movement stage の guard / support regression を追加する</name>
  <files>tests/application/world/services/test_world_simulation_movement_stage_service.py</files>
  <behavior>
    - Test 1: movement service が未注入なら no-op で終わる。
    - Test 2: active pursuit があるとき、continuation が movement 実行前に評価される。
    - Test 3: actor busy、map 不在、actor 不在、goal/pursuit 不在などの skip 条件で movement が呼ばれない。
  </behavior>
  <action>`WorldSimulationMovementStageService` を直接叩く新規 test file を作る。ここでは facade 全体の順序を再確認しないで、movement stage 単体の support 有無と skip 条件に絞ること。既存 `test_world_simulation_service.py` にある pursuit continuation 系 expectation と矛盾しないよう、"何が呼ばれるか / 呼ばれないか" を狭く確認する。新しい world state factory を hand-roll しすぎず、最低限の status / map / actor stub で済ませること。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_movement_stage_service.py -x</automated>
  </verify>
  <done>movement stage に対する guard regression が独立 file で存在し、将来の変更で pursuit continuation や skip 条件の退行を小さく検知できる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: monster lifecycle stage の handoff regression を追加する</name>
  <files>tests/application/world/services/test_world_simulation_monster_lifecycle_stage_service.py, tests/application/world/services/test_hunger_migration.py</files>
  <behavior>
    - Test 1: spawn slot support 有無で `process_spawn_and_respawn_by_slots` と legacy respawn の分岐が切り替わる。
    - Test 2: hunger migration support と survival coordinator が無いとき、blocked actor ids は空で返る。
    - Test 3: active spot だけが survival coordinator に渡され、返った blocked actor ids がそのまま返される。
  </behavior>
  <action>`WorldSimulationMonsterLifecycleStageService` の新規 test file を作り、spawn/respawn 分岐と survival handoff を小さく固定する。既存 `test_hunger_migration.py` は apply flow の integration regression として残しつつ、stage 自体の境界は facade 経由ではなく direct test へ寄せる。Phase 13 は migration の新 capability 追加ではないため、destination selection の枝分かれや細かい foraging policy をここで広げないこと。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_monster_lifecycle_stage_service.py tests/application/world/services/test_hunger_migration.py -k "migration or survival or respawn" -x</automated>
  </verify>
  <done>lifecycle stage の spawn/survival handoff が小粒 regression で守られ、hunger migration integration との役割分担も明確になる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: monster behavior stage の guard regression を追加して coordinator test と役割分担を固定する</name>
  <files>tests/application/world/services/test_world_simulation_monster_behavior_stage_service.py, tests/application/world/services/test_monster_behavior_coordinator.py</files>
  <behavior>
    - Test 1: inactive spot の actor は処理されない。
    - Test 2: skipped actor / busy actor / active-time 不一致 actor は coordinator に渡らない。
    - Test 3: 処理対象 actor だけが distance order に従って coordinator に渡る。
  </behavior>
  <action>`WorldSimulationMonsterBehaviorStageService` の新規 test file を作り、stage 側が持つ guard 契約だけを直接守る。`tests/application/world/services/test_monster_behavior_coordinator.py` は coordinator 本体の一本道順序・save 契約・pursuit failure semantics を守る位置として維持し、stage test ではその中身を再検証しないこと。Phase 13 では "guard は stage、主処理は coordinator" という境界を test suite 上でも読める形に揃える。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_monster_behavior_stage_service.py tests/application/world/services/test_monster_behavior_coordinator.py -x</automated>
  </verify>
  <done>monster behavior stage と coordinator の責務境界が test suite 上でも明示され、busy/skipped/active-time regression を小さく検知できる。</done>
</task>

</tasks>

<verification>
Plan 13-02 完了時点で、movement / lifecycle / behavior の各 stage が direct test で guard 契約を持ち、既存 policy / coordinator regression と組み合わせて WSTEST-02 を満たせることを確認する。
</verification>

<success_criteria>
world simulation の今後の分割で、facade-level integration を壊したのか、stage guard を壊したのか、coordinator/policy 本体を壊したのかを test file 単位で切り分けられる。
</success_criteria>

<output>
After completion, create `.planning/phases/13-simulation-regression-harness/13-simulation-regression-harness-02-SUMMARY.md`
</output>
