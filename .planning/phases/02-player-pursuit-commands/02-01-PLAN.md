---
phase: 02-player-pursuit-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/world/contracts/commands.py
  - src/ai_rpg_world/application/world/services/pursuit_command_service.py
  - src/ai_rpg_world/application/world/exceptions/command/pursuit_command_exception.py
  - tests/application/world/contracts/test_commands.py
  - tests/application/world/services/test_pursuit_command_service.py
autonomous: true
requirements:
  - PURS-01
user_setup: []
must_haves:
  truths:
    - Phase 2 adds an explicit application entrypoint for player pursuit start instead of overloading static movement commands.
    - Pursuit start accepts only currently visible player or monster targets and rejects missing, invisible, invalid, self, or busy-state requests without mutating pursuit state.
    - Starting pursuit clears in-progress static destination/path state while leaving continuation, observation wiring, and tick-driven pursuit logic for later phases.
    - Reissuing start for the same target is handled as refresh/idempotent success, while switching targets is orchestrated at the service layer.
  artifacts:
    - path: src/ai_rpg_world/application/world/services/pursuit_command_service.py
      provides: Application service for explicit pursuit start orchestration and validation
      min_lines: 60
    - path: src/ai_rpg_world/application/world/contracts/commands.py
      provides: Start pursuit command DTO for typed application entry
      contains: StartPursuitCommand
    - path: src/ai_rpg_world/application/world/exceptions/command/pursuit_command_exception.py
      provides: Typed pursuit start validation failures with stable error codes
      min_lines: 20
    - path: tests/application/world/services/test_pursuit_command_service.py
      provides: Start-command regression coverage for visible-target validation and path clearing
      min_lines: 60
  key_links:
    - from: src/ai_rpg_world/application/world/services/pursuit_command_service.py
      to: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      via: Service delegates lifecycle mutation to aggregate-owned pursuit methods
      pattern: start_pursuit|update_pursuit|cancel_pursuit|clear_path
    - from: src/ai_rpg_world/application/world/services/pursuit_command_service.py
      to: src/ai_rpg_world/application/world/services/player_current_state_builder.py
      via: Busy-state truth and visible-world validation stay aligned with existing current-state rules
      pattern: busy
    - from: src/ai_rpg_world/application/world/services/pursuit_command_service.py
      to: src/ai_rpg_world/application/world/services/visible_object_read_model_builder.py
      via: Visible target resolution reuses existing LOS-filtered read-model logic
      pattern: visible
---

<objective>
Add the explicit player pursuit start command and its validation boundary for Phase 2.

Purpose: Give the application layer a deterministic, testable entrypoint for starting pursuit against currently visible actors without pulling continuation-loop or observation concerns forward.
Output: Start pursuit command DTOs/exceptions, a dedicated application service, and regression tests for validation and state transitions.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-player-pursuit-commands/02-CONTEXT.md
@.planning/phases/02-player-pursuit-commands/02-RESEARCH.md
@.planning/phases/02-player-pursuit-commands/02-VALIDATION.md
@.planning/phases/01-pursuit-domain-vocabulary/01-01-PLAN.md
@.planning/phases/01-pursuit-domain-vocabulary/01-02-PLAN.md
@.planning/phases/01-pursuit-domain-vocabulary/01-03-PLAN.md
@src/ai_rpg_world/application/world/services/movement_service.py
@src/ai_rpg_world/application/world/contracts/commands.py
@src/ai_rpg_world/application/world/services/world_query_service.py
@src/ai_rpg_world/application/world/services/player_current_state_builder.py
@src/ai_rpg_world/application/world/services/visible_object_read_model_builder.py
@src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
@tests/application/world/services/test_movement_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the typed pursuit start command surface</name>
  <files>src/ai_rpg_world/application/world/contracts/commands.py, src/ai_rpg_world/application/world/exceptions/command/pursuit_command_exception.py, tests/application/world/contracts/test_commands.py</files>
  <action>Add a frozen `StartPursuitCommand` DTO and a pursuit-specific command exception type that matches existing application command patterns. Keep DTO validation limited to scalar shape and identifier sanity, and reserve visibility, target-kind, self-target, placement, and busy checks for the service layer. Define stable `error_code` values for the Phase 2 start failure cases so later LLM tooling can surface them without re-encoding validation rules.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/contracts/test_commands.py -q -k "pursuit or command"</verify>
  <done>The application layer has a typed start-pursuit command and pursuit-specific error vocabulary aligned with existing command-service conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pursuit start orchestration and validation</name>
  <files>src/ai_rpg_world/application/world/services/pursuit_command_service.py, tests/application/world/services/test_pursuit_command_service.py</files>
  <action>Create a dedicated pursuit command service adjacent to movement instead of hiding start behavior inside `MovementApplicationService`. Reuse existing repositories/read-model helpers to confirm the actor exists, is placed, is not busy, and can currently see the target by `world_object_id`; allow only player or monster targets and reject self-targets. On success, clear static path/destination state, then either start a new pursuit, refresh the same target with latest visible snapshot, or cancel the old pursuit before starting a different target. Keep all mutation inside `PlayerStatusAggregate` methods and do not add tick-based continuation, last-known refresh automation beyond start/refresh, or observation wiring here.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_pursuit_command_service.py -q -k "start"</verify>
  <done>The start-pursuit use case validates visible targets, handles refresh/switch semantics, and leaves continuation logic for later phases.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in Phase 2 start regressions</name>
  <files>tests/application/world/services/test_pursuit_command_service.py</files>
  <action>Add focused service-level regressions for visible player success, visible monster success, missing target, invisible target, invalid target kind, self-target, missing placement, busy-state rejection, static-path clearing on success, same-target refresh, and different-target switch. Keep the tests on the application boundary; reuse existing aggregate and visibility tests as assumptions instead of expanding Phase 1 domain coverage.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_pursuit_command_service.py -q</verify>
  <done>Regression coverage fixes the explicit Phase 2 contract for pursuit start without drifting into cancel-tool or wiring scope.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/world/contracts/test_commands.py tests/application/world/services/test_pursuit_command_service.py -q`
- [ ] Start validation covers missing target, invisible target, invalid kind, self-target, missing placement, and busy-state rejection
- [ ] Successful start clears static destination/path state without adding tick continuation or observation wiring
- [ ] Same-target refresh and different-target switch semantics are exercised at the service level
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a dedicated pursuit start entrypoint that validates visible targets and reflects correct aggregate/event behavior without leaking into later-phase continuation or LLM wiring work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-player-pursuit-commands/02-01-SUMMARY.md`
</output>
