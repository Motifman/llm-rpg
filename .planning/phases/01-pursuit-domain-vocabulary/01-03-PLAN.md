---
phase: 01-pursuit-domain-vocabulary
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
  - src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
  - docs/pursuit/phase1_integration_strategy.md
  - tests/domain/player/aggregate/test_player_status_aggregate.py
  - tests/domain/monster/aggregate/test_monster_aggregate.py
  - tests/application/world/services/test_movement_service.py
autonomous: true
requirements:
  - OUTC-02
  - RUNT-02
user_setup: []
must_haves:
  truths:
    - Player pursuit state is stored independently from static destination and planned-path state.
    - Player aggregate methods can start, update, fail, and cancel pursuit while emitting the new lifecycle events.
    - Phase 1 leaves an explicit monster-side integration strategy and touchpoint that maps existing chase/search vocabulary toward the new pursuit model without prematurely implementing Phase 5 behavior.
    - Clearing or advancing static movement path does not implicitly erase pursuit state unless pursuit-specific logic says so.
  artifacts:
    - path: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      provides: Aggregate-owned pursuit state and lifecycle helpers
      min_lines: 40
    - path: docs/pursuit/phase1_integration_strategy.md
      provides: Cross-actor integration strategy for player and monster state models
      min_lines: 25
    - path: tests/domain/player/aggregate/test_player_status_aggregate.py
      provides: Aggregate-level pursuit separation and event emission coverage
      min_lines: 60
    - path: tests/domain/monster/aggregate/test_monster_aggregate.py
      provides: Regression coverage for the documented monster touchpoint
      min_lines: 20
    - path: tests/application/world/services/test_movement_service.py
      provides: Regression protection against movement-path coupling
      min_lines: 20
  key_links:
    - from: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      to: src/ai_rpg_world/domain/pursuit/value_object/pursuit_state.py
      via: Dedicated aggregate field for pursuit state
      pattern: PursuitState
    - from: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      to: src/ai_rpg_world/domain/pursuit/event/pursuit_events.py
      via: Aggregate emits lifecycle events with add_event
      pattern: Pursuit.*Event
    - from: docs/pursuit/phase1_integration_strategy.md
      to: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      via: Documented monster alignment touchpoint and migration path
      pattern: TargetSpottedEvent|TargetLostEvent|BehaviorStateEnum
---

<objective>
Integrate the new pursuit vocabulary into the player state model and document the monster-side alignment strategy without collapsing pursuit into destination movement.

Purpose: Complete the Phase 1 domain contract by proving the pursuit state lives on the player aggregate separately from static movement while also making the Phase 5 monster-alignment path explicit in a concrete strategy artifact.
Output: `PlayerStatusAggregate` pursuit fields/helpers, a monster integration strategy document, and regression tests for both touchpoints.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pursuit-domain-vocabulary/01-CONTEXT.md
@.planning/phases/01-pursuit-domain-vocabulary/01-RESEARCH.md
@.planning/phases/01-pursuit-domain-vocabulary/01-01-SUMMARY.md
@.planning/phases/01-pursuit-domain-vocabulary/01-02-SUMMARY.md
@src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
@src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
@src/ai_rpg_world/domain/monster/event/monster_events.py
@src/ai_rpg_world/domain/monster/enum/monster_enum.py
@src/ai_rpg_world/application/world/services/movement_service.py
@tests/domain/player/aggregate/test_player_status_aggregate.py
@tests/domain/monster/aggregate/test_monster_aggregate.py
@tests/application/world/services/test_movement_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend PlayerStatusAggregate with optional pursuit state</name>
  <files>src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py, tests/domain/player/aggregate/test_player_status_aggregate.py</files>
  <action>Add pursuit-related fields to `PlayerStatusAggregate` using safe defaults so existing constructors and fixtures keep working. Expose read accessors and aggregate methods for starting pursuit, refreshing meaningful target-state changes, failing pursuit, and cancelling pursuit. Keep the new state separate from `_current_destination`, `_planned_path`, and `goal_*`; do not make movement-path helpers mutate pursuit implicitly in this phase.</action>
  <verify>pytest tests/domain/player/aggregate/test_player_status_aggregate.py -q -k pursuit</verify>
  <done>The player aggregate can own pursuit state and manipulate it without breaking existing constructor call sites or reusing static movement fields.</done>
</task>

<task type="auto">
  <name>Task 2: Emit pursuit lifecycle events and capture the monster alignment strategy</name>
  <files>src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py, docs/pursuit/phase1_integration_strategy.md, tests/domain/player/aggregate/test_player_status_aggregate.py, tests/domain/monster/aggregate/test_monster_aggregate.py</files>
  <action>Wire the new player aggregate methods to emit the pursuit started, updated, failed, and cancelled events via `add_event(...)`, following existing player and monster event emission style. In parallel, create a concise strategy artifact that names the current monster-side touchpoints (`BehaviorStateEnum`, `TargetSpottedEvent`, `TargetLostEvent`, relevant aggregate state) and states how Phase 5 should map them onto the new neutral pursuit vocabulary without changing monster runtime behavior yet. Add a light monster regression or characterization test only if needed to lock the referenced touchpoint in place.</action>
  <verify>pytest tests/domain/player/aggregate/test_player_status_aggregate.py tests/domain/monster/aggregate/test_monster_aggregate.py -q -k \"pursuit or target or state\"</verify>
  <done>Player aggregate methods produce the new lifecycle events, and the repository contains an explicit monster alignment strategy artifact tied to real monster-side code touchpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Add regressions that protect movement and pursuit separation</name>
  <files>tests/domain/player/aggregate/test_player_status_aggregate.py, tests/application/world/services/test_movement_service.py, docs/pursuit/phase1_integration_strategy.md</files>
  <action>Add tests proving that destination/path clearing still behaves as static movement logic, while pursuit state persists until pursuit-specific fail/cancel handling runs. Use a lightweight application-service regression only where it locks the separation boundary; do not integrate full tick continuation or command handling yet. Ensure the strategy document explicitly records that monster alignment is a Phase 5 follow-through on the Phase 1 vocabulary, not a hidden runtime implementation in this plan.</action>
  <verify>pytest tests/domain/player/aggregate/test_player_status_aggregate.py tests/application/world/services/test_movement_service.py -q</verify>
  <done>Regression coverage demonstrates `RUNT-02`, and the plan leaves a concrete, scoped strategy artifact for monster-side state-model alignment.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/domain/player/aggregate/test_player_status_aggregate.py tests/domain/monster/aggregate/test_monster_aggregate.py tests/application/world/services/test_movement_service.py -q`
- [ ] Existing `PlayerStatusAggregate(...)` constructor call sites still work with default pursuit fields
- [ ] Pursuit state and lifecycle events are reachable from the player aggregate without changing observation wiring or world tick logic
- [ ] `docs/pursuit/phase1_integration_strategy.md` names the monster-side alignment touchpoints and defers behavior implementation to Phase 5
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 proves pursuit state is aggregate-owned, eventful, and separate from static movement path state, with an explicit monster integration strategy artifact for later alignment
  </success_criteria>

<output>
After completion, create `.planning/phases/01-pursuit-domain-vocabulary/01-03-SUMMARY.md`
</output>
