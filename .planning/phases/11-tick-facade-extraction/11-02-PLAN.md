---
phase: 11-tick-facade-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_hit_box_stage_service.py
  - tests/application/world/services/test_world_simulation_service.py
  - tests/application/llm/test_llm_wiring_integration.py
autonomous: true
requirements:
  - WSIM-01
  - WSIM-02
must_haves:
  truths:
    - "active spot freeze を共有入力として、monster lifecycle・monster behavior・HitBox 更新が facade から個別 stage に委譲される。"
    - "observation、actor 処理順、LLM / reflection scheduling の副作用タイミングが分割後も既存 runtime path と両立する。"
    - "post-tick wiring は UoW 外で実行され、WorldSimulationService が外部公開契約の差し替え点として残る。"
  artifacts:
    - path: "src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py"
      provides: "spawn/respawn と hunger migration を含む lifecycle stage"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      provides: "active spot 上の actor behavior stage"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_hit_box_stage_service.py"
      provides: "HitBox 更新 stage"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      provides: "active spot 集約と post-tick wiring を残した facade"
  key_links:
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py"
      via: "active_spot_ids と current_tick の委譲"
      pattern: "_monster_lifecycle_stage"
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      via: "距離順 actor behavior 処理"
      pattern: "_monster_behavior_stage"
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "tests/application/llm/test_llm_wiring_integration.py"
      via: "UoW 外 post-tick wiring 契約"
      pattern: "run_scheduled_turns|run_after_tick"
---

<objective>
active spot 以降の tick stage と post-tick wiring を facade から明示的に分け、Phase 11 の 6-stage split を behavior-safe incremental に閉じる。

Purpose: WSIM-01/02 の中心である lifecycle / behavior / HitBox の委譲境界と、副作用タイミングの wiring 契約を同時に固定する。
Output: 後半 3 stage service、active spot 共有入力、UoW 外 post-tick wiring を残した facade。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tick-facade-extraction/11-CONTEXT.md
@.planning/phases/11-tick-facade-extraction/11-RESEARCH.md
@.planning/phases/11-tick-facade-extraction/11-01-PLAN.md
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@tests/application/world/services/test_world_simulation_service.py
@tests/application/llm/test_llm_wiring_integration.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
active_spot_ids: Set[SpotId] = {pm.spot_id for pm in player_map_map.values()}
for physical_map in maps:
    if physical_map.spot_id not in active_spot_ids:
        continue
```

Post-tick contract:
```python
if self._llm_turn_trigger is not None:
    self._llm_turn_trigger.run_scheduled_turns()
if self._reflection_runner is not None:
    self._reflection_runner.run_after_tick(current_tick)
```

Regression anchors:
```python
def test_actors_processed_in_order_of_distance_to_player_when_player_on_map(...): ...
def test_only_active_spot_gets_build_observation_and_save(...): ...
def test_inactive_spot_actors_never_get_build_observation(...): ...
def test_dead_monster_respawns_when_interval_elapsed_and_condition_met(...): ...
def test_tick_calls_llm_turn_trigger_run_scheduled_turns_when_provided(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: active spot 以降の 3 stage を抽出して facade を order coordinator に寄せる</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_monster_lifecycle_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_hit_box_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_service.py</files>
  <behavior>
    - Test 1: active spot に限定した observation/build/save 契約が維持される。
    - Test 2: actor 処理順は player からの距離順のまま維持される。
    - Test 3: dead monster respawn と HitBox 更新が facade 経由で継続する。
  </behavior>
  <action>Plan 01 の facade を前提に、monster lifecycle、monster behavior、HitBox 更新を `application/world/services/` 直下の 3 stage service へ切り出す。`active_spot_ids` の計算、map 再取得タイミング、UoW 境界、例外変換は facade に残す。hunger migration は Phase 12 の policy 抽出に進めず、lifecycle stage の内部 helper として扱う。behavior stage は既存の `_actors_sorted_by_distance_to_players` と `_process_single_actor_behavior` の順序契約を守ること。HitBox stage は既存 collision/save ループを壊さないこと。`simulation/` subpackage への再編はしない。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_service.py -k "actors_processed_in_order_of_distance_to_player_when_player_on_map or only_active_spot_gets_build_observation_and_save or inactive_spot_actors_never_get_build_observation or dead_monster_respawns_when_interval_elapsed_and_condition_met" -x</automated>
  </verify>
  <done>後半 3 stage が facade collaborator として導入され、active spot / actor order / respawn / HitBox 契約が既存回帰 slice で守られる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: post-tick wiring と facade 回帰スライスを固定する</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_service.py, tests/application/world/services/test_world_simulation_service.py, tests/application/llm/test_llm_wiring_integration.py</files>
  <behavior>
    - Test 1: `llm_turn_trigger.run_scheduled_turns()` は tick の末尾で 1 回だけ呼ばれる。
    - Test 2: `reflection_runner.run_after_tick(current_tick)` は UoW 脱出後の facade path に残る。
    - Test 3: wiring integration から見て `WorldSimulationService` が既存差し替え点のまま使える。
  </behavior>
  <action>`WorldSimulationService` の末尾にある `llm_turn_trigger` と `reflection_runner` の実行を facade の post-tick hook として明示し、stage service 側へ移さない。既存 `test_world_simulation_service.py` と `test_llm_wiring_integration.py` を最小限更新し、wiring 契約が facade のまま通ることを回帰スライスで示す。reflection の追加アサートは Phase 11 の目的に必要な範囲に限り、広い test deepening には広げない。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_service.py tests/application/llm/test_llm_wiring_integration.py -k "llm_turn_trigger or WorldSimulationService" -x</automated>
  </verify>
  <done>post-tick wiring が facade 外部契約として固定され、LLM / reflection scheduling の回帰 guard が既存 integration path で green になる。</done>
</task>

</tasks>

<verification>
Phase 11 全体の gate として、world simulation facade が 6 stage を順序どおり委譲しつつ、observation と LLM / reflection scheduling のタイミングを壊していないことを既存 regression slice で確認する。
</verification>

<success_criteria>
`WorldSimulationService` が facade / order coordinator として残り、environment、movement、harvest、monster lifecycle、monster behavior、HitBox の 6 stage が明示的 collaborator として読める。active spot・actor order・post-tick wiring の既存契約が regression で維持される。
</success_criteria>

<output>
After completion, create `.planning/phases/11-tick-facade-extraction/11-tick-facade-extraction-02-SUMMARY.md`
</output>
