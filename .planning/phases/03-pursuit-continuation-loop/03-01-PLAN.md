---
phase: 03-pursuit-continuation-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - RUNT-01
  - PURS-03
user_setup: []
must_haves:
  truths:
    - Phase 3 introduces a pursuit-aware continuation prepass inside the existing world tick instead of creating a second movement loop or scheduler.
    - Active pursuit is evaluated before the normal one-step movement advance, and ordinary non-pursuit movement behavior stays unchanged.
    - Busy actors keep their pursuit state but skip continuation work for that tick without being failed, cancelled, or replanned.
    - An active pursuit with no static path is treated as needing same-tick continuation handling rather than being silently skipped by the world tick.
    - This plan stops at runtime orchestration and pre-movement decision routing; Phase 4 observation delivery and LLM re-trigger wiring remain out of scope.
  artifacts:
    - path: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      provides: Focused application-layer helper for tick-time pursuit continuation decisions
      min_lines: 80
    - path: src/ai_rpg_world/application/world/services/world_simulation_service.py
      provides: World tick integration point that invokes pursuit continuation before movement execution
      contains: _advance_pending_player_movements
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: Integration coverage for pursuit-aware tick ordering, busy skip, and active-pursuit pathless handling
      min_lines: 80
  key_links:
    - from: src/ai_rpg_world/application/world/services/world_simulation_service.py
      to: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      via: World tick delegates pursuit-specific continuation decisions to a dedicated helper before movement execution
      pattern: pursue|continuation
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/application/world/services/world_query_service.py
      via: Continuation reuses canonical player current-state visibility and busy-state read models
      pattern: get_player_current_state|visible
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/application/world/services/movement_service.py
      via: Continuation prepares pursuit movement state but still hands one-step execution to the existing movement service
      pattern: tick_movement_in_current_unit_of_work|set_destination
---

<objective>
Add the Phase 3 runtime continuation orchestrator at the world-tick seam.

Purpose: Ensure active pursuit is considered on every eligible world tick before the existing movement step runs, while preserving current tick ordering and non-pursuit behavior.
Output: A pursuit-aware continuation helper integrated into `WorldSimulationApplicationService` plus world-tick tests that lock in ordering and skip semantics.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-pursuit-continuation-loop/03-CONTEXT.md
@.planning/phases/03-pursuit-continuation-loop/03-RESEARCH.md
@.planning/phases/03-pursuit-continuation-loop/03-VALIDATION.md
@.planning/phases/02-player-pursuit-commands/02-01-PLAN.md
@.planning/phases/02-player-pursuit-commands/02-02-PLAN.md
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/application/world/services/movement_service.py
@src/ai_rpg_world/application/world/services/world_query_service.py
@src/ai_rpg_world/application/world/services/pursuit_command_service.py
@src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
@tests/application/world/services/test_world_simulation_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a dedicated pursuit continuation helper for world-tick use</name>
  <files>src/ai_rpg_world/application/world/services/pursuit_continuation_service.py</files>
  <action>Create a focused application service or helper dedicated to Phase 3 tick-time pursuit continuation. It should accept the actor/player state needed to evaluate active pursuit, return a narrow decision/result for the tick, and keep world-tick orchestration thin. Reuse `WorldQueryService.get_player_current_state(...)` as the visibility source of truth and reserve actual failure semantics and detailed replanning outcomes for the next plan, but make the helper shape explicitly ready for those branches.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "pursuit and continuation"</verify>
  <done>A dedicated continuation seam exists so Phase 3 logic is not buried directly inside `WorldSimulationApplicationService`.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate continuation prepass into the existing player-movement tick stage</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_service.py, tests/application/world/services/test_world_simulation_service.py</files>
  <action>Extend `_advance_pending_player_movements(...)` so it no longer keys only on `goal_spot_id`. For each player status, distinguish plain movement from active pursuit, run pursuit continuation before the existing one-step movement call, and preserve the current skip behavior for missing actors/maps. Keep busy handling explicit: if a player has active pursuit but is busy, do not fail, cancel, or replan the pursuit on that tick. Continue to let `tick_movement_in_current_unit_of_work(...)` own the actual one-step move.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "pursuit and tick"</verify>
  <done>The world tick runs pursuit continuation at the correct time and still routes actual movement through the existing movement service.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in tick-order and pathless-pursuit regressions</name>
  <files>tests/application/world/services/test_world_simulation_service.py</files>
  <action>Add integration tests proving that pursuit continuation happens before movement execution, that non-pursuit movement still advances as before, that busy actors preserve pursuit state without continuation work, and that an actor with active pursuit but no current path is still processed by the pursuit continuation stage instead of being skipped. Use call-order or effect assertions that make the runtime sequence readable in test names and failures.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or movement"</verify>
  <done>Phase 3 runtime orchestration is covered by tests that would fail if pursuit continuation moved later in the tick or stopped handling pathless pursuit states.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or movement"`
- [ ] World tick evaluates active pursuit before calling `tick_movement_in_current_unit_of_work(...)`
- [ ] Busy actors retain pursuit state and skip continuation work without failure or cancel side effects
- [ ] Active pursuit with no current movement path is still considered during the tick
- [ ] No observation/LLM delivery wiring is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 gains a deterministic runtime seam for pursuit continuation inside the existing world tick without altering the one-step movement engine or crossing into Phase 4 responsibilities
  </success_criteria>

<output>
After completion, create `.planning/phases/03-pursuit-continuation-loop/03-01-SUMMARY.md`
</output>
