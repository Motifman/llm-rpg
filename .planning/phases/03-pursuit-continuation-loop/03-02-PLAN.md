---
phase: 03-pursuit-continuation-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
  - src/ai_rpg_world/application/world/services/movement_service.py
  - tests/application/world/services/test_world_simulation_service.py
  - tests/application/world/services/test_movement_service.py
autonomous: true
requirements:
  - PURS-03
  - PURS-04
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - Visible-target pursuit refresh updates `target_snapshot` and `last_known` only when the target coordinate or spot meaningfully changes, avoiding per-tick event spam.
    - Losing sight of the target freezes the previous `last_known` and continues pursuit toward that destination until success, replanning failure, or structured pursuit failure.
    - Phase 3 failure reasons are resolved inside continuation logic with the exact semantics chosen in context: `target_missing`, `path_unreachable`, and `vision_lost_at_last_known`, with `target_missing` reserved for targets that are actually absent from the authoritative world-state lookup rather than merely invisible.
    - Replanning uses the existing movement/pathfinding engine instead of inventing pursuit-only movement execution, even if a narrow coordinate-oriented helper must be added.
    - This plan must not wire pursuit outcomes into observation recipients, event registries, or LLM turn delivery.
  artifacts:
    - path: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      provides: Continuation state machine for visible refresh, frozen last-known pursuit, and structured failure decisions
      min_lines: 140
    - path: src/ai_rpg_world/application/world/services/movement_service.py
      provides: Reusable path-replanning support for pursuit continuation without bypassing the movement engine
      min_lines: 260
    - path: tests/application/world/services/test_movement_service.py
      provides: Regression coverage for pursuit-oriented replanning and path-clearing edge cases
      min_lines: 60
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: Integration coverage for visible refresh, frozen last-known continuation, and structured failure results
      min_lines: 120
  key_links:
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
      via: Continuation mutates pursuit lifecycle only through aggregate-owned update and failure methods
      pattern: update_pursuit|fail_pursuit
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/application/world/services/movement_service.py
      via: Pursuit continuation delegates path creation or refresh to the canonical movement/pathfinding layer
      pattern: destination|path|tick_movement
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
      via: Runtime continuation maps Phase 3 stop conditions to structured pursuit failure reasons
      pattern: target_missing|path_unreachable|vision_lost_at_last_known
    - from: src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
      to: src/ai_rpg_world/domain/world/repository/physical_map_repository.py
      via: Continuation distinguishes invisible-but-present targets from truly missing targets using authoritative world presence lookup
      pattern: find_by_spot_id|get_object
---

<objective>
Implement the Phase 3 continuation decision logic for visible targets, frozen `last_known`, and structured failure outcomes.

Purpose: Turn the runtime seam from 03-01 into a complete pursuit continuation state machine that refreshes pursuit state, replans only when necessary, and fails cleanly when continuation is no longer possible.
Output: Continuation logic and movement-path integration that satisfy visible-target follow, last-known continuation, and failure semantics for Phase 3.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-pursuit-continuation-loop/03-CONTEXT.md
@.planning/phases/03-pursuit-continuation-loop/03-RESEARCH.md
@.planning/phases/03-pursuit-continuation-loop/03-VALIDATION.md
@.planning/phases/03-pursuit-continuation-loop/03-01-PLAN.md
@src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
@src/ai_rpg_world/application/world/services/movement_service.py
@src/ai_rpg_world/application/world/services/world_query_service.py
@src/ai_rpg_world/domain/player/aggregate/player_status_aggregate.py
@src/ai_rpg_world/domain/pursuit/enum/pursuit_failure_reason.py
@tests/application/world/services/test_world_simulation_service.py
@tests/application/world/services/test_movement_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement visible-target refresh and frozen last-known continuation</name>
  <files>src/ai_rpg_world/application/world/services/pursuit_continuation_service.py, tests/application/world/services/test_world_simulation_service.py</files>
  <action>Teach the continuation service to resolve the active target from current visibility, refresh `target_snapshot` and `last_known` only on meaningful coordinate or spot change, and preserve the previous visible snapshot when the target is no longer visible. When sight is lost, continue toward the frozen `last_known` instead of clearing pursuit immediately. Keep the decision order explicit and testable so each tick can be explained as visible refresh, spot-change refresh, frozen last-known continuation, or failure.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "last_known or visible or pursuit_updated or spot"</verify>
  <done>Continuation distinguishes visible-target refresh from frozen last-known pursuit and updates pursuit state only when the target meaningfully moves by coordinate or spot.</done>
</task>

<task type="auto">
  <name>Task 2: Add pursuit-compatible replanning support through the movement layer</name>
  <files>src/ai_rpg_world/application/world/services/movement_service.py, src/ai_rpg_world/application/world/services/pursuit_continuation_service.py, tests/application/world/services/test_movement_service.py</files>
  <action>Add the narrowest movement-service or pathfinding adapter needed so pursuit continuation can request path refreshes toward a visible coordinate or frozen `last_known` destination without abusing unrelated object/location semantics. Reuse existing path calculation and stored movement fields, keep one-step movement execution unchanged, and make path-empty vs path-unreachable behavior explicit so continuation can decide whether to replan or fail.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_movement_service.py -q -k "pursuit or destination or path"</verify>
  <done>Pursuit continuation can refresh movement paths through the existing movement engine without introducing a parallel pursuit-only mover.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and verify structured Phase 3 failure semantics</name>
  <files>src/ai_rpg_world/application/world/services/pursuit_continuation_service.py, tests/application/world/services/test_world_simulation_service.py, tests/application/world/services/test_movement_service.py</files>
  <action>Implement the three required failure branches inside continuation: fail with `target_missing` when an authoritative world-presence lookup can no longer resolve the target as present, fail with `path_unreachable` when visible-target or frozen-last-known replanning cannot produce a usable path, and fail with `vision_lost_at_last_known` when the actor reaches the frozen last-known position without reacquiring visibility. Add regressions that separate `invisible but still present` from `target_missing`, save aggregate state through the normal repository path, and assert that pursuit failure clears pursuit state but does not retroactively conflate movement cancellation with pursuit cancellation.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py tests/application/world/services/test_movement_service.py -q -k "target_missing or unreachable or vision_lost or invisible"</verify>
  <done>Phase 3 continuation resolves all required structured failures with tests that separate visible-target, invisible-but-present, frozen-last-known, and unreachable-path behavior.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py tests/application/world/services/test_movement_service.py -q -k "pursuit or last_known or unreachable or target_missing"`
- [ ] Visible target movement updates `target_snapshot` and `last_known` only on meaningful coordinate or spot change
- [ ] Lost-visibility pursuit continues toward frozen `last_known` instead of failing immediately
- [ ] `target_missing`, `path_unreachable`, and `vision_lost_at_last_known` are each covered by explicit regressions, with `invisible but present` distinguished from `target_missing`
- [ ] One-step movement still executes through `tick_movement_in_current_unit_of_work(...)`
- [ ] No observation pipeline or LLM delivery wiring is added
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 can continue pursuit toward both currently visible targets and frozen last-known positions with structured failure reasons and minimal movement-layer expansion
  </success_criteria>

<output>
After completion, create `.planning/phases/03-pursuit-continuation-loop/03-02-SUMMARY.md`
</output>
