# シナリオ試験用入力

このディレクトリには、実 LLM 実験では使わず、世界機構の回帰試験だけで使う
シナリオ JSON を置く。

実験対象は `data/scenarios/` に置き、機構を最小構成で検査する入力はここに置く。
試験用入力を実験プロファイルや実験用コマンドから参照しない。

`darkened_station.json` は `station_drill.json` の派生元として、削除した場所や対象を
文章が案内していないことを比較検査するために保持している。
