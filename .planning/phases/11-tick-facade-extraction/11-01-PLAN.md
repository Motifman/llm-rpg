---
phase: 11-tick-facade-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_environment_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_harvest_stage_service.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - WSIM-01
  - WSIM-02
must_haves:
  truths:
    - "WorldSimulationService が tick entry point と UoW 境界を維持したまま、環境処理・継続移動・採取完了を明示的な stage service に委譲できる。"
    - "pursuit continuation -> movement の順序と movement/harvest 後の map 再取得契約が facade 上で読み取れる。"
  artifacts:
    - path: "src/ai_rpg_world/application/world/services/world_simulation_environment_stage_service.py"
      provides: "天候更新と map weather 同期の stage service"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py"
      provides: "pursuit continuation を含む pending movement stage"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_harvest_stage_service.py"
      provides: "due harvest 完了 stage"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      provides: "facade の順序制御と前半 stage 委譲"
  key_links:
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py"
      via: "tick 内の continuation 後 movement 委譲"
      pattern: "_movement_stage"
    - from: "src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/pursuit_continuation_service.py"
      via: "継続追跡判定"
      pattern: "evaluate_tick"
    - from: "src/ai_rpg_world/application/world/services/world_simulation_service.py"
      to: "src/ai_rpg_world/application/world/services/world_simulation_harvest_stage_service.py"
      via: "harvest 後の map 再取得"
      pattern: "_harvest_stage"
---

<objective>
`WorldSimulationService` を facade に寄せ始め、tick 前半の world progression を明示的な stage service に委譲できるようにする。

Purpose: 6-stage split の最初の incremental step として、順序回帰リスクが高い environment / movement / harvest を facade から読み取れる委譲境界に変える。
Output: 前半 3 stage service と、それらを使う facade の薄い orchestration。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tick-facade-extraction/11-CONTEXT.md
@.planning/phases/11-tick-facade-extraction/11-RESEARCH.md
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@tests/application/world/services/test_world_simulation_service.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
class WorldSimulationApplicationService:
    def tick(self) -> WorldTick: ...
    def _tick_impl(self) -> WorldTick: ...
```

Current order contract inside `_tick_impl()`:
```python
current_tick = self._time_provider.advance_tick()
latest_weather = self._update_weather_if_needed(current_tick)
maps = self._physical_map_repository.find_all()
for physical_map in maps:
    self._sync_weather_to_map(physical_map, latest_weather)
if self._movement_service is not None:
    self._advance_pending_player_movements(current_tick)
    maps = self._physical_map_repository.find_all()
if self._harvest_command_service is not None:
    self._complete_due_harvests(maps, current_tick)
    maps = self._physical_map_repository.find_all()
```

Regression anchors from tests/application/world/services/test_world_simulation_service.py:
```python
def test_tick_runs_pursuit_continuation_before_movement_execution(...): ...
def test_tick_auto_completes_due_player_harvest(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 前半 stage service の契約と composition を導入する</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_environment_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_movement_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_harvest_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_service.py</files>
  <behavior>
    - Test 1: facade から environment / movement / harvest が個別 collaborator として読める。
    - Test 2: movement stage は pursuit continuation を movement 実行前に評価する。
    - Test 3: movement と harvest の各 stage 実行後に facade が最新 maps を再取得できる。
  </behavior>
  <action>既存 private method を足場にして、`application/world/services/` 直下へ `WorldSimulationEnvironmentStageService`、`WorldSimulationMovementStageService`、`WorldSimulationHarvestStageService` を追加する。Phase 11 では clean cut な API 再設計をしないで、既存依存を薄く受け取って既存ロジックを安全に包むこと。`WorldSimulationApplicationService` には tick entry point、UoW 境界、active spot 集約の準備、例外変換だけを残し、天候更新・weather sync・pending movement・due harvest は新 stage へ委譲する。`pursuit continuation -> movement` の順序を逆転させないこと。movement/harvest 後の `find_all()` 再取得は消さないこと。`ToolArgumentResolver` や policy 抽出には踏み込まないこと。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_service.py -k "pursuit_continuation_before_movement_execution or tick_auto_completes_due_player_harvest" -x</automated>
  </verify>
  <done>facade が前半 3 stage を collaborator として持ち、tick 前半の順序がコード上で読める。既存 pursuit continuation と harvest の回帰テストが通る。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: facade 回帰テストを最小限更新して前半委譲境界を固定する</name>
  <files>tests/application/world/services/test_world_simulation_service.py</files>
  <behavior>
    - Test 1: facade refactor 後も pursuit continuation が movement より前で呼ばれる。
    - Test 2: harvest 自動完了が facade 経由で継続し、stage 導入で契約が壊れない。
  </behavior>
  <action>既存 `test_world_simulation_service.py` の前半順序系テストを facade 抽出後の seam に合わせて必要最小限だけ更新する。Phase 11 は test deepening が目的ではないため、新しい stage 単体テスト群は増やさず、既存 facade regression が実装の導線として残るように調整する。stage の内部実装詳細に過度に依存するアサートは避け、外部から見える tick order 契約に寄せる。</action>
  <verify>
    <automated>python -m pytest tests/application/world/services/test_world_simulation_service.py -k "pursuit_continuation_before_movement_execution or tick_auto_completes_due_player_harvest" -x</automated>
  </verify>
  <done>前半 stage 抽出後も facade regression が green で、前半委譲境界が既存 test slice で守られている。</done>
</task>

</tasks>

<verification>
前半 stage 導入後に `WorldSimulationService` が facade として残り、tick 前半の委譲順序がコードと既存回帰テストの両方で読めることを確認する。
</verification>

<success_criteria>
environment / movement / harvest の 3 stage が新規 service として存在し、`WorldSimulationService` から直接 private method 本体を追わなくても前半 world progression の順序が理解できる。
</success_criteria>

<output>
After completion, create `.planning/phases/11-tick-facade-extraction/11-tick-facade-extraction-01-SUMMARY.md`
</output>
