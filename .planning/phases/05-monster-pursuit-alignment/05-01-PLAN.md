---
phase: 05-monster-pursuit-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
  - src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - tests/domain/monster/aggregate/test_monster_aggregate.py
  - tests/domain/monster/service/test_behavior_state_transition_service.py
  - tests/application/world/services/test_world_simulation_service.py
autonomous: true
requirements:
  - PURS-02
user_setup: []
must_haves:
  truths:
    - Monster pursuit is aligned to the shared pursuit model without replacing monster-local `BehaviorStateEnum`; `CHASE` and `SEARCH` remain runtime labels, while pursuit target and last-known semantics become explicit and inspectable.
    - Entering `SEARCH` after vision loss still means the monster is pursuing the same target, so target identity and frozen last-known information must survive the lose-target transition.
    - Pursuit may start from either spotted-target or attacked-by flows inside the existing monster behavior pipeline; no command-layer API is introduced.
    - Leaving pursuit states for `FLEE`, `RETURN`, or another non-pursuit state clears active monster pursuit instead of silently leaving stale aligned state behind.
    - This plan stops at state alignment and transition correctness; explicit last-known failure completion and exhaustive regressions are reserved for the next plan.
  artifacts:
    - path: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      provides: Aggregate-owned monster pursuit-alignment state and lifecycle synchronization with behavior transitions
      min_lines: 80
    - path: src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
      provides: Transition outputs that preserve enough target context to carry pursuit into SEARCH
      min_lines: 30
    - path: src/ai_rpg_world/application/world/services/world_simulation_service.py
      provides: Runtime integration point where aligned monster pursuit begins from existing observation and transition flow
      contains: _process_single_actor_behavior
    - path: tests/domain/monster/aggregate/test_monster_aggregate.py
      provides: Aggregate regressions for pursuit start, search retention, and pursuit clearing on state exit
      min_lines: 80
    - path: tests/application/world/services/test_world_simulation_service.py
      provides: Integration proof that visible monster pursuit still starts through normal world tick behavior
      min_lines: 60
  key_links:
    - from: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      to: src/ai_rpg_world/domain/pursuit/value_object/pursuit_state.py
      via: Monster runtime state aligns with the shared pursuit vocabulary for target and last-known semantics
      pattern: PursuitState|last_known|target_snapshot
    - from: src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
      to: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      via: Transition results must preserve enough context for aggregate-applied SEARCH pursuit alignment
      pattern: do_lose_target|last_known_coordinate|target_id
    - from: src/ai_rpg_world/application/world/services/world_simulation_service.py
      to: src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
      via: World tick remains the single runtime seam where monster observations become pursuit-aligned state changes
      pattern: process_single_actor_behavior|apply_behavior_transition
---

<objective>
Align monster runtime state with the shared pursuit foundation during Phase 5.

Purpose: Make monster pursuit target retention and start semantics explicit within the existing behavior pipeline so `CHASE` and `SEARCH` can be reasoned about with the same target/last-known concepts as player pursuit.
Output: Monster aggregate and transition logic that keep pursuit-aligned state in sync with existing behavior-state transitions, plus focused aggregate and runtime regressions.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-monster-pursuit-alignment/05-CONTEXT.md
@.planning/phases/05-monster-pursuit-alignment/05-RESEARCH.md
@.planning/phases/05-monster-pursuit-alignment/05-VALIDATION.md
@.planning/phases/01-pursuit-domain-vocabulary/01-03-PLAN.md
@.planning/phases/03-pursuit-continuation-loop/03-02-PLAN.md
@src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py
@src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py
@src/ai_rpg_world/domain/pursuit/value_object/pursuit_state.py
@src/ai_rpg_world/domain/pursuit/value_object/pursuit_last_known_state.py
@src/ai_rpg_world/domain/pursuit/value_object/pursuit_target_snapshot.py
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@tests/domain/monster/aggregate/test_monster_aggregate.py
@tests/domain/monster/service/test_behavior_state_transition_service.py
@tests/application/world/services/test_world_simulation_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add monster-side pursuit-alignment state and helpers</name>
  <files>src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py, tests/domain/monster/aggregate/test_monster_aggregate.py</files>
  <action>Extend `MonsterAggregate` with an explicit pursuit-aligned representation that reuses the shared pursuit value objects instead of inventing monster-only target/last-known structures. Keep `BehaviorStateEnum` intact as the monster-local runtime label, but add accessors and synchronization points so code can tell when the monster has active pursuit, who the target is, and what the frozen last-known position is. Starting from spotted-target and attacked-by flows should enter aligned pursuit; entering non-pursuit states like `FLEE` or `RETURN` should clear it.</action>
  <verify>.venv/bin/python -m pytest tests/domain/monster/aggregate/test_monster_aggregate.py -q -k "pursuit or chase or search or target_lost"</verify>
  <done>The monster aggregate exposes a pursuit-aligned state that stays consistent with its existing chase/search behavior labels.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve target identity and last-known through lose-target transitions</name>
  <files>src/ai_rpg_world/domain/monster/service/behavior_state_transition_service.py, src/ai_rpg_world/domain/monster/aggregate/monster_aggregate.py, tests/domain/monster/service/test_behavior_state_transition_service.py, tests/domain/monster/aggregate/test_monster_aggregate.py</files>
  <action>Change the lose-target path so the `CHASE` -> `SEARCH` transition preserves the target identity and frozen last-known information needed for active pursuit, instead of clearing them immediately. Keep the monster event pattern intact, but make sure transition outputs and aggregate application leave enough state behind for Phase 5 search semantics. Add domain-level regressions that fail if `SEARCH` starts with no target or no last-known anchor.</action>
  <verify>.venv/bin/python -m pytest tests/domain/monster/service/test_behavior_state_transition_service.py tests/domain/monster/aggregate/test_monster_aggregate.py -q -k "lose_target or search or pursuit"</verify>
  <done>Vision loss now produces a valid SEARCH pursuit state instead of erasing the information Phase 5 needs.</done>
</task>

<task type="auto">
  <name>Task 3: Keep world-tick monster pursuit start on the existing behavior seam</name>
  <files>src/ai_rpg_world/application/world/services/world_simulation_service.py, tests/application/world/services/test_world_simulation_service.py</files>
  <action>Adjust the monster behavior runtime flow only where needed so aligned pursuit state is entered and saved through the normal observation -> transition -> aggregate-apply sequence. Reuse the current world tick seam; do not add a parallel monster pursuit service or command entrypoint. Add an integration regression showing that a monster spotting or reacting to an attacker enters the aligned pursuit path during normal simulation.</action>
  <verify>.venv/bin/python -m pytest tests/application/world/services/test_world_simulation_service.py -q -k "monster and pursuit"</verify>
  <done>Monster pursuit starts through the existing world simulation flow and is visible in integration tests.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/domain/monster/aggregate/test_monster_aggregate.py tests/domain/monster/service/test_behavior_state_transition_service.py tests/application/world/services/test_world_simulation_service.py -q -k "pursuit or chase or search or target_lost"`
- [ ] `CHASE` and `SEARCH` are both representable as active monster pursuit without replacing monster-local behavior states
- [ ] Losing sight of a target no longer erases the target identity or frozen last-known data needed for `SEARCH`
- [ ] Monster pursuit starts from existing spotted-target and attacked-by behavior flows only
- [ ] Entering `FLEE` or `RETURN` clears aligned pursuit state instead of leaving stale target data behind
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 gains an explicit monster pursuit-alignment model that preserves target and last-known semantics across `CHASE` and `SEARCH` while staying inside the existing monster behavior pipeline
  </success_criteria>

<output>
After completion, create `.planning/phases/05-monster-pursuit-alignment/05-01-SUMMARY.md`
</output>
