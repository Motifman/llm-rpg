---
phase: 12-monster-policy-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/world/services/hunger_migration_policy.py
  - src/ai_rpg_world/application/world/services/monster_foraging_rule.py
  - src/ai_rpg_world/application/world/services/monster_pursuit_failure_rule.py
  - src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py
  - src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
  - src/ai_rpg_world/application/world/services/world_simulation_service.py
  - tests/application/world/services/test_hunger_migration_policy.py
  - tests/application/world/services/test_monster_behavior_coordinator.py
autonomous: true
requirements:
  - WSPOL-01
  - WSPOL-02
must_haves:
  truths:
    - "飢餓移住の『候補を選ぶ』判定が repository を触らない純粋 policy として単独で評価できる。"
    - "monster behavior は『観測を作る -> 状態を決める -> 失敗を確定する -> 行動を解く -> 記録する』の一本道で読める。"
    - "foraging と pursuit failure の判定位置が action resolver から分離され、Phase 5 の pursuit semantics を保ったまま明示される。"
  artifacts:
    - path: "src/ai_rpg_world/application/world/services/hunger_migration_policy.py"
      provides: "facts ベースで移住対象を 1 体選ぶ pure policy"
    - path: "src/ai_rpg_world/application/world/services/monster_foraging_rule.py"
      provides: "feed facts から visible feed と selected feed target を返す専任 rule"
    - path: "src/ai_rpg_world/application/world/services/monster_pursuit_failure_rule.py"
      provides: "pre/post action の pursuit failure 判定"
    - path: "src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py"
      provides: "monster behavior の一本道 coordinator"
    - path: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      provides: "actor loop と busy/active gating を保った behavior stage entry"
  key_links:
    - from: "src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py"
      to: "src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py"
      via: "stage から actor ごとの behavior 実行を委譲"
      pattern: "MonsterBehaviorCoordinator|process_actor_behavior"
    - from: "src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py"
      to: "src/ai_rpg_world/application/world/services/monster_foraging_rule.py"
      via: "foraging facts 作成と feed target 解決"
      pattern: "foraging"
    - from: "src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py"
      to: "src/ai_rpg_world/application/world/services/monster_pursuit_failure_rule.py"
      via: "action 前後の pursuit failure 判定"
      pattern: "evaluate_pre_action|evaluate_post_action"
---

<objective>
monster behavior と hunger migration candidate selection を、Phase 11 の stage 境界を保ったまま読める rule / coordinator へ切り出す。

Purpose: WSPOL-01 の pure policy 抽出と WSPOL-02 の behavior 読み口改善を、world simulation facade を壊さない incremental step として先に固定する。
Output: pure hunger migration policy、foraging / pursuit failure rule、monster behavior coordinator、その unit regression。
</objective>

<execution_context>
@/Users/minagawa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minagawa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-monster-policy-separation/12-CONTEXT.md
@.planning/phases/12-monster-policy-separation/12-RESEARCH.md
@.planning/phases/12-monster-policy-separation/12-VALIDATION.md
@.planning/phases/11-tick-facade-extraction/11-tick-facade-extraction-02-SUMMARY.md
@src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/application/world/services/monster_action_resolver.py
@tests/application/world/services/test_world_simulation_service.py

<interfaces>
From src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py:
```python
class WorldSimulationMonsterBehaviorStageService:
    def run(
        self,
        maps: List[PhysicalMapAggregate],
        active_spot_ids: Set[SpotId],
        current_tick: WorldTick,
    ) -> None: ...
```

Current behavior seam in src/ai_rpg_world/application/world/services/world_simulation_service.py:
```python
visible_feed, selected_feed_target = self._build_feed_observation(...)
transition_result = BehaviorStateTransitionService().compute_transition(...)
failure_reason = self._resolve_monster_pursuit_failure_reason(...)
action = resolver.resolve_action(monster, observation, actor.coordinate)
if self._should_fail_monster_search_at_last_known(...):
    monster.fail_pursuit(...)
```

Current resolver contract in src/ai_rpg_world/application/world/services/monster_action_resolver.py:
```python
class MonsterActionResolverImpl:
    def resolve_action(
        self,
        monster: MonsterAggregate,
        observation: BehaviorObservation,
        actor_coordinate: Coordinate,
    ) -> BehaviorAction: ...
```

Regression anchors in tests/application/world/services/test_world_simulation_service.py:
```python
def test_tick_visible_monster_target_starts_aligned_pursuit_state(...): ...
def test_tick_search_fails_with_target_missing_when_target_disappears(...): ...
def test_tick_search_keeps_same_pursuit_when_target_reappears(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 12 の pure rule/coordinator unit anchors を先に作る</name>
  <files>tests/application/world/services/test_hunger_migration_policy.py, tests/application/world/services/test_monster_behavior_coordinator.py</files>
  <behavior>
    - Test 1: hunger migration policy は eligible candidates から hunger 最大の 1 体だけを選び、同率は入力順を保つ。
    - Test 2: preferred feed が無い、閾値未満、spot に feed がある候補は migration 対象にならない。
    - Test 3: behavior coordinator は foraging -> observation -> transition -> failure -> resolver -> record/save の順を守る。
    - Test 4: `TARGET_MISSING` と `VISION_LOST_AT_LAST_KNOWN` の pursuit semantics は coordinator + rule の組み合わせで維持される。
  </behavior>
  <action>Phase 12 の Wave 0 として `test_hunger_migration_policy.py` と `test_monster_behavior_coordinator.py` を新規作成する。test 名は requirements と phase decisions が読める粒度にし、Plan 01 実装の契約を先に固定すること。hunger migration test では repository や map aggregate を使わず facts DTO だけで評価できる形にする。behavior coordinator test では action resolver を作り変えず、mock/stub で coordinator の一本道順序と pursuit failure の pre/post checkpoints を固定する。Phase 12 は capability 追加ではないため、新 state や destination selection 改善を期待値に含めない。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_hunger_migration_policy.py tests/application/world/services/test_monster_behavior_coordinator.py -x</automated>
  </verify>
  <done>Phase 12 の pure policy / coordinator contracts を表す 2 つの unit test file が存在し、red から着手できる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hunger migration policy を facts-based pure policy として抽出する</name>
  <files>src/ai_rpg_world/application/world/services/hunger_migration_policy.py, tests/application/world/services/test_hunger_migration_policy.py</files>
  <behavior>
    - Test 1: hunger migration policy は 1 tick に 1 体だけ返す。
    - Test 2: hunger の最大値で migrant を選び、同率時は新しい tie-break を導入しない。
    - Test 3: policy 入力は candidate facts のみで、repository / provider / transition service 依存を持たない。
  </behavior>
  <action>`application/world/services/` 直下に hunger migration candidate DTO と `HungerMigrationPolicy` を追加する。policy は「hunger が threshold 以上」「preferred feed がある」「spot に feed が無い」を facts で受け取り、eligible candidates から最も hunger が高い 1 体だけを返すこと。接続先選定、gateway lookup、map transition、repository save は一切入れない。tie-break は Python の既存入力順維持に委ね、複雑な優先順位は増やさない。実装後、Task 1 の unit test を green にする。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_hunger_migration_policy.py -x</automated>
  </verify>
  <done>`HungerMigrationPolicy` が pure module として存在し、WSPOL-01 の candidate selection 部分を repository 非依存で評価できる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: monster behavior を一本道 coordinator と専任 rule に整理して stage へ接続する</name>
  <files>src/ai_rpg_world/application/world/services/monster_foraging_rule.py, src/ai_rpg_world/application/world/services/monster_pursuit_failure_rule.py, src/ai_rpg_world/application/world/services/monster_behavior_coordinator.py, src/ai_rpg_world/application/world/services/world_simulation_monster_behavior_stage_service.py, src/ai_rpg_world/application/world/services/world_simulation_service.py, tests/application/world/services/test_monster_behavior_coordinator.py</files>
  <behavior>
    - Test 1: foraging rule が visible feed / selected feed target を作り、behavior coordinator が observation へ渡す。
    - Test 2: pursuit failure rule が pre-action の `TARGET_MISSING` と post-action の `VISION_LOST_AT_LAST_KNOWN` を区別する。
    - Test 3: behavior stage は busy 判定と active-time gate を維持しつつ、actor ごとの処理本体を coordinator へ委譲する。
  </behavior>
  <action>`_build_feed_observation()` と pursuit failure helper 群をそのまま別クラスへ移すのではなく、Phase 12 decision に合わせて `MonsterForagingRule`、`MonsterPursuitFailureRule`、`MonsterBehaviorCoordinator` の 3 役に分ける。coordinator は `観測を作る -> 状態を決める -> 失敗を確定する -> 行動を解く -> 記録する` の順で上から読める実装にし、判定本体は foraging / pursuit failure rule へ委譲する。action resolver の責務拡大や語彙変更はしない。`WorldSimulationMonsterBehaviorStageService` は actor loop と active-time gating を保ち、`WorldSimulationService` では behavior stage への接続先を coordinator ベースへ差し替える。starvation / old-age はこの plan で behavior stage から消し切らなくてよいが、Task 3 で新規 behavior coordinator に持ち込まないこと。</action>
  <verify>
    <automated>uv run python -m pytest tests/application/world/services/test_monster_behavior_coordinator.py tests/application/world/services/test_world_simulation_service.py -k "visible_monster_target_starts_aligned_pursuit_state or target_missing or reappears" -x</automated>
  </verify>
  <done>monster behavior の主処理が coordinator と専任 rule を通して読めるようになり、foraging / pursuit failure の責務位置が facade と action resolver の間で明示される。</done>
</task>

</tasks>

<verification>
Plan 01 完了時点で、hunger migration の candidate selection は pure policy として単独検証でき、monster behavior は stage entry と resolver のあいだで一本道 coordinator と専任 rule に整理されていることを unit と既存 world simulation regression で確認する。
</verification>

<success_criteria>
`WorldSimulationService` の monster behavior seam を追うと、foraging と pursuit failure を含む業務ルールの責務境界が private helper 群ではなく明示的 collaborator から読める。hunger migration は apply を除いた選定ロジックだけ pure policy として独立している。
</success_criteria>

<output>
After completion, create `.planning/phases/12-monster-policy-separation/12-monster-policy-separation-01-SUMMARY.md`
</output>
