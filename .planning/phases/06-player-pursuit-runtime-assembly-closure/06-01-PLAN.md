---
phase: 06-player-pursuit-runtime-assembly-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_rpg_world/application/llm/bootstrap.py
  - src/ai_rpg_world/presentation/player_pursuit_runtime.py
  - tests/application/llm/test_llm_wiring_integration.py
autonomous: true
requirements:
  - PURS-05
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - Phase 6 closes a composition gap, not a pursuit-semantics gap; the plan must establish one authoritative non-test runtime assembly path instead of adding new pursuit behavior.
    - The authoritative runtime path must assemble both `PursuitCommandService` and `PursuitContinuationService` into the same player-facing runtime so pursuit start/cancel and tick continuation cannot drift apart.
    - The runtime bootstrap must wire `create_llm_agent_wiring(...)` with a non-null `pursuit_command_service` and `WorldSimulationApplicationService` with a non-null `pursuit_continuation_service`.
    - The new composition seam must make silent omission of pursuit wiring harder, either by fail-fast checks or by a dedicated pursuit-capable builder API with a narrow contract.
    - This plan stops at authoritative runtime assembly and contract coverage; full start-to-observation execution proof belongs to subsequent plans.
  artifacts:
    - path: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      provides: Concrete non-test bootstrap that assembles a pursuit-capable player runtime package
      min_lines: 80
    - path: src/ai_rpg_world/application/llm/bootstrap.py
      provides: Narrow bootstrap support for the new authoritative runtime entrypoint without breaking existing generic composition helpers
      contains: compose_llm_runtime
    - path: tests/application/llm/test_llm_wiring_integration.py
      provides: Contract tests proving the authoritative runtime path injects pursuit command and continuation services together
      min_lines: 80
  key_links:
    - from: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      to: src/ai_rpg_world/application/llm/bootstrap.py
      via: The new authoritative runtime path should build on the existing LLM bootstrap instead of duplicating wiring logic
      pattern: compose_llm_runtime|create_llm_agent_wiring
    - from: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      to: src/ai_rpg_world/application/world/services/pursuit_command_service.py
      via: Runtime composition must expose player pursuit start/cancel through the same live runtime path
      pattern: PursuitCommandService
    - from: src/ai_rpg_world/presentation/player_pursuit_runtime.py
      to: src/ai_rpg_world/application/world/services/world_simulation_service.py
      via: Runtime composition must pass a concrete continuation service into world tick assembly
      pattern: pursuit_continuation_service
---

<objective>
Create the authoritative non-test runtime assembly path for player pursuit.

Purpose: Replace today's ad hoc optional wiring with one composition entrypoint that always assembles the pursuit-capable player runtime correctly.
Output: A concrete runtime bootstrap module plus contract tests that prove the assembled runtime contains both player pursuit command and continuation services.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-RESEARCH.md
@.planning/phases/06-player-pursuit-runtime-assembly-closure/06-VALIDATION.md
@src/ai_rpg_world/application/llm/bootstrap.py
@src/ai_rpg_world/application/llm/wiring/__init__.py
@src/ai_rpg_world/application/world/services/pursuit_command_service.py
@src/ai_rpg_world/application/world/services/pursuit_continuation_service.py
@src/ai_rpg_world/application/world/services/world_simulation_service.py
@src/ai_rpg_world/infrastructure/events/event_handler_composition.py
@tests/application/llm/test_llm_wiring_integration.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Introduce one authoritative player pursuit runtime bootstrap</name>
  <files>src/ai_rpg_world/presentation/player_pursuit_runtime.py, src/ai_rpg_world/application/llm/bootstrap.py</files>
  <action>Create a concrete runtime composition module under `presentation` that assembles the player-facing pursuit runtime from existing seams. It should instantiate or receive `PursuitCommandService`, `PursuitContinuationService`, `create_llm_agent_wiring(...)`, `EventHandlerComposition`, and `WorldSimulationApplicationService` through one narrow builder API. Keep `compose_llm_runtime(...)` reusable, but document and support the new higher-level entrypoint as the authoritative pursuit-capable runtime path.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "compose_llm_runtime or bootstrap"</verify>
  <done>A single non-test bootstrap exists that a caller can use to obtain a pursuit-capable player runtime without manually stitching partial dependencies together.</done>
</task>

<task type="auto">
  <name>Task 2: Make pursuit capability explicit in the runtime contract</name>
  <files>src/ai_rpg_world/presentation/player_pursuit_runtime.py, tests/application/llm/test_llm_wiring_integration.py</files>
  <action>Ensure the new runtime bootstrap cannot silently omit either pursuit seam. Prefer a dedicated result object or fail-fast validation that proves `pursuit_command_service` and `pursuit_continuation_service` were both assembled for the player pursuit runtime path. Preserve generic low-level helpers for other runtime profiles without forcing pursuit globally.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "pursuit_tool or pursuit_command_service or continuation_service"</verify>
  <done>The runtime contract makes pursuit capability observable and testable instead of relying on optional-argument convention.</done>
</task>

<task type="auto">
  <name>Task 3: Add bootstrap contract coverage for assembled pursuit services</name>
  <files>tests/application/llm/test_llm_wiring_integration.py</files>
  <action>Add integration tests that instantiate the new non-test bootstrap and assert: pursuit tools are enabled on the assembled wiring path, the returned world simulation service holds a non-null pursuit continuation service, and the observation registry is attached to the same runtime. The test must prove the assembled path, not a test-local hand-built equivalent.</action>
  <verify>.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "bootstrap and pursuit"</verify>
  <done>Regression coverage fails if future changes reintroduce a half-wired runtime where only start/cancel or only continuation is assembled.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/application/llm/test_llm_wiring_integration.py -q -k "pursuit or compose_llm_runtime or bootstrap"`
- [ ] The new runtime entrypoint lives under `src/` and is not test-only
- [ ] The assembled runtime wires both `pursuit_command_service` and `pursuit_continuation_service`
- [ ] The runtime contract makes missing pursuit capability observable instead of silently degraded
- [ ] No new pursuit semantics or observation schema changes are introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has one documented, pursuit-capable player runtime assembly path that closes the audit's non-test composition gap at the bootstrap level
  </success_criteria>

<output>
After completion, create `.planning/phases/06-player-pursuit-runtime-assembly-closure/06-01-SUMMARY.md`
</output>
